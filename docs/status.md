# Platform Status

> Living state tracker. Updated in-place as work progresses.

**Last updated**: 2026-06-23 (NW-142 accepted as stopped; consumed PC2 fixture reconciliation selected)

---

## Current Routing

Use this section as the low-token bootstrap for new sessions.

- Active implementation gate: none; NW-142 accepted as a stopped `NOT_READY` proof attempt and exactly one consumed-fixture reconciliation successor selected
- Active process/control slice: consumed PC2 live-proof fixture reconciliation route selected as NW-143
- Last accepted NW: NW-142 PC2 live browser walkthrough proof attempt
- Current blocker: PC2 live browser proof is `NOT_READY`; retained isolated PC2 stack and synthetic auth path remain available, but the only NW-141 unresolved attention fixture was already cleared by accepted resolution event `bcafd46a-d439-4548-b51d-bc8ec9dddb09` before NW-142 could record bounded before/after browser proof; real users/data still require NW-093 first
- Next evidence command: execute `docs/agent-working-surface/prompts/NW-143-reconcile-consumed-pc2-fixture-and-select-proof-retry-path.md`.
- Do-not-start-next-product-work-until: complete or park NW-143; do not broaden parked PC1-PC4 proof/implementation standing into reporting/import/export, warehouse/API/catalog, queue/list/batch/automation, pattern projection work, new scope, retention/security/offboarding promises, entity lifecycle, tenant/control-plane, R12 mutation, all-PC proof, or real users/data by drift.
- Gap register review standing: reviewed 2026-06-22; NW-139 did not mutate the gap register and preserved NW-044, NW-045, NW-073, NW-053, NW-054, NW-093, NW-021, NW-036, and NW-094 through NW-098 as trigger-based routes.
- Artifact trace standing: NW-125, NW-126, NW-139, NW-140, NW-141, and NW-142 artifacts indexed; NW-143 prompt exists as the selected consumed-fixture reconciliation successor; NW-128 and NW-134 outputs are platform specifications, not artifacts; NW-129, NW-135, and NW-138 outputs are runtime implementation/tests/status standing, not artifacts; NW-130, NW-131, NW-132, NW-136, and NW-137 artifacts indexed; NW-133 output is a product PM handoff, not an artifact.
- Last updated: 2026-06-23

### Current Routing Detail

Use this detail when the control panel above is not enough.

- Phase 4 is complete; the completion audit is green.
- Post-Phase-4 stabilization now uses `docs/agent-working-surface/README.md` as the active agent working-surface router.
- Architecture-sensitive future work now routes through `docs/agent-working-surface/decision-anchor-layer/README.md` for DEC anchors and gap routing. The old architecture rationale companion is retired as an active surface.
- New durable documentation outputs route through `docs/documentation-organization.md`: accepted product/platform behavior belongs under `docs/specifications/`, while operational policies, runbooks, and rehearsal evidence belong under `docs/operations/`. Prompts and working-surface artifacts remain routing inputs, not final accepted homes.
- Current implementation acceptance status belongs in `docs/agent-working-surface/baseline-acceptance-register.md`; do not treat legacy Phase 4 review drafts as active baseline truth.
- `docs/flagged-positions.md` is legacy gated-risk provenance, not the current pressure intake path. FP-004's underlying new-envelope-field concern is tracked through BAR-107 and architecture gap classification when concrete pressure appears.
- Review findings and technical debt are pressure to classify, not automatic architecture work. If the fix stays inside accepted boundaries and changes no contract, durable spec, authority primitive, stored interpretation, or accepted standing, route it as an implementation/tooling gap and track promoted work through the NW backlog, bounded prompt when useful, code/tests, and the commit trace. If the finding exposes missing accepted behavior or boundary ambiguity, route it through the gap playbook first.
- Current baseline standing: BAR-001 through BAR-015 and BAR-104 are accepted, including BAR-010 config package delivery and BAR-104 production OIDC/JWT/Keycloak authority.
- Scenario runtime evidence now includes NW-025/S19, NW-026/S00, NW-029/S21, NW-030/S27, NW-032/S23, NW-033/S26, and NW-042/S22. Use the backlog rows for exact evidence.
- Contract hygiene now includes root schemas for deployer-authored shape format and the server-emitted/mobile-consumed config package: `contracts/shape-format.schema.json` and `contracts/config-package.schema.json`.
- The 2026-06-16 durable behavior routing audit is the active working-surface evidence for scattered IDR-era protocol notes: implementation evidence can prove a behavior is load-bearing, but it cannot preserve implemented drift over CDL/exploration/right-position correction. Use `docs/agent-working-surface/artifacts/architecture-classification-drift-audit.md`, `docs/agent-working-surface/artifacts/idr-durable-surface-routing-audit.md`, and the gap-routing playbook classification re-test rule before relying on IDR prose as normative.
- NW-068 is accepted in commit `3b4dfb0`: accepted configuration, expression, and config-package behavior now has durable platform specs at `docs/specifications/platform/expression-language.md` and `docs/specifications/platform/configuration-package-and-shapes.md`; `/api/sync/config` auth/ETag/`If-None-Match` semantics are recorded in `contracts/sync-protocol.md`; no runtime behavior, JSON schemas, fixtures, BAR, CDL, or old IDR text changed.
- NW-069 is accepted: accepted assignment payload meaning, assignment identity, scope composition, write-time location scope, scope-filtered sync behavior, activity role-action boundary, multi-axis assignment containment, assignment-admin command capability, non-goals, and escalation triggers now have a durable platform spec at `docs/specifications/platform/assignment-scope-and-administration.md`. No runtime behavior, payload schemas, envelope fields/types, scope mechanisms, activity action vocabulary, authority source, `contracts/sync-protocol.md`, BAR, CDL, old IDR text, or phase files changed. Future-dated `valid_from` activation remains explicitly not product/API-ready without follow-up evidence; NW-053 remains the future-decision route for new subject/query/custom scope mechanisms.
- NW-070 is accepted: accepted authenticated-actor resolution, explicit `(issuer, subject) -> actor_id` principal binding, OIDC/JWKS validation boundary, bearer-bound API actor context, push actor-ref equality, mobile actor alignment, deployment-managed principal-binding provisioning, operation-history/projection semantics, operations split, non-authority boundaries, non-goals, and escalation triggers now have a durable platform/security spec at `docs/specifications/platform/production-auth-principal-binding.md`. No runtime behavior, sync protocol semantics, JSON schemas, fixtures, BAR, CDL, old IDR text, phase files, operations policy/runbook/rehearsal records, online admin APIs, IdP claim authority, new actor authority sources, or real-production standing changed.
- Post-NW-068 durable-behavior extraction follow-ups are tracked in `docs/agent-working-surface/platform-next-work-backlog.md`: NW-069 assignment/scope/admin, NW-070 production auth/principal binding, NW-071 shared-device session/local state, NW-072 conflict/flag resolution and operational attention query boundary, and NW-074 old-doc/reference cleanup are accepted, while NW-073 pattern registry/projection remains a candidate row. These rows do not block NW-067; use the backlog trigger map to select one only when it becomes the active bounded work item or a downstream task would otherwise rely on IDR prose as normative.
- NW-074 is accepted in commit `4e0447a`: active routers/status, decision-anchor rows, module-interface references, decision indexes, the IDR-018 false `contracts/expression.schema.json` trace, and active server/mobile source comments now route accepted NW-068 through NW-070 behavior through the durable platform specs and contracts before IDR provenance. No runtime behavior, contract semantics, schemas, fixtures, tests, BAR, CDL, operations standing, old phase chronology, or NW-071 through NW-073 standing changed.
- NW-079 is accepted: production web admin authentication and admin-authority now have a durable platform/security spec at `docs/specifications/platform/production-web-admin-authentication-and-authority.md`. The selected model is OIDC/JWKS browser login with a server-managed session cookie, server-resolved explicit-principal-bound admin actor context, command-specific web/config admin authority, S23 author/validate/readiness-review/approve/publish/responsibility authority split, append-only audit posture, and continued development-console containment. No runtime code, UI, sessions, filters, APIs, contracts, schemas, online principal-binding admin, mobile login, reporting/export, retention/security, conflict batch handling, new scopes, or production deployment approval changed. Successor implementation rows must be promoted separately before production web admin UI work.
- NW-082 is accepted as a non-binding Product Candidate 1 routing artifact at `docs/agent-working-surface/artifacts/NW-082-product-candidate-1-milestone-boundary-and-multi-tenancy-routing.md`. It does not accept product behavior, tenant architecture, contract authority, implementation, or production readiness.
- NW-083 is accepted for routing only: Product Candidate 1 uses the reversible managed-isolation lane of one customer-facing Organization mapped to one managed single-tenant Datarun deployment with one internal/default Workspace. Tenant-aware auth, workspace-scoped config, tenant sync context, mobile local partition keys, storage backfills, pooled `tenant_id` predicates, and envelope changes remain successor routes. Next actionable work is NW-084, the Product Candidate 1 product specification under `docs/specifications/product/`.
- NW-084 is accepted as the Product Candidate 1 product specification at `docs/specifications/product/product-candidate-1.md`. Product Candidate 1 is a one-Organization/default-Workspace candidate with accepted user-visible actors, terms, setup/admin/mobile journeys, explicit exclusions, platform/contract/operations guardrails, and product acceptance criteria. It does not accept runtime implementation, tenant-aware internals, mobile OIDC, reporting/export, retention/security, operations evidence, or real-production approval.
- NW-086 is accepted after repair commit `b6388c4`: `/web-admin` now provides the separate production web-admin browser login/session boundary with OIDC authorization-code login initiation, browser-style GET callback, server-side token exchange, `OidcJwksTokenValidator` id-token validation, server-stored state plus nonce validation, configured public callback URI, explicit active principal binding before session creation, server-managed session context, `Secure`/`HttpOnly`/`SameSite=Lax` production cookie settings, CSRF-protected protected POSTs, logout, expiry and principal-binding revalidation denial, and secret-safe login/session audit events. Production also fails closed without explicit HTTPS web-admin authorization, token, and redirect URIs plus client id/secret. Existing bearer-token APIs remain owned by `ActorTokenInterceptor`; the current Thymeleaf `/admin`, `/admin/config`, and `/admin/dev` lanes remain development-only and hidden in production. No web/config admin command policy, config UI, assignment admin UX, online binding admin, mobile login, tenant-aware internals, contracts/schemas/envelopes, migrations, BAR, CDL, operations evidence, or real-production approval changed.
- NW-087 is accepted in commit `b8b4f7c`: added the deployment-configured server-side `admin_command_capabilities` policy for explicit actor UUIDs and platform-owned `web_admin.access`, `web_admin.read_scoped`, and `config_admin.*` command names. `/web-admin/shell` now requires a valid web-admin session plus `web_admin.access` for the server-resolved session actor, denied actors receive a 403 without browser-auth challenge, and shell grant/deny audit events remain secret-safe. Missing, malformed, or unknown-command policy fails closed for evaluation. IdP claims/groups/roles/JWT `actor_id`, request/UI actor IDs, fixed development actors, and assignment roles do not grant shell access; `web_admin.access` grants no config-admin, assignment-admin, resolver, or data-read authority by itself. Existing bearer APIs remain owned by `ActorTokenInterceptor`, and current `/admin`, `/admin/config`, and `/admin/dev` lanes remain development-only and hidden in production. No config workflow/admin UI, online binding admin, mobile login/session, tenant-aware internals, deployment provisioning manifest, contracts/schemas/envelopes, migrations, BAR, CDL, operations evidence, or real-production approval changed.
- NW-100 is accepted in commit `d99a8dc`: the reviewed `config-publish` one-shot provisioning manifest now requires, validates, and applies server-side `admin_command_capabilities` transactionally alongside existing deployment config rows. Exact reapply remains idempotent, invalid/unknown command policy rejects without partial publication, and the policy remains deployment config only, not config-package/mobile authority. No online admin APIs/UI, contracts/schemas/migrations, tenant/mobile behavior, BAR, CDL, operations evidence, real-production approval, or NW-088 config workflow changed.
- NW-088 is accepted in commit `476f5ec`: the S23 setup workflow is now productized as server-rendered Spring MVC/Thymeleaf/HTML under production `/web-admin/config`, behind the accepted web-admin session boundary, exact `web_admin.access` shell gate, and exact `config_admin.author`, `config_admin.validate`, `config_admin.readiness_review`, `config_admin.approve`, and `config_admin.publish` command gates. It supports draft candidate author/update, dry-run validation, readiness review, exact candidate-hash approval, publish through `ReviewedConfigProvisioner`, monotonic audit/history, stale-hash guards, and current package status. The JSON candidate editor is accepted as the first vertical; richer structured editing and polish remain future work. Verification: escalated DB-access run of `./mvnw -Dtest=WebAdminConfigWorkflowIntegrationTest,WebAdminSessionBoundaryTest,AdminCommandCapabilityServiceIntegrationTest,OneShotProvisioningIntegrationTest,ConfigIntegrationTest,DeployTimeValidatorTest,ProductionDevelopmentSurfaceFilterTest,WebAdminSecurityFoundationTest test` passed 129 tests, 0 failures, 0 errors, 0 skipped; `git diff --check` passed. No Angular/SPA, `/admin/config` productionization, browser editing of admin/assignment command policy or principal-binding policy, contracts/schemas/mobile/tenant internals, operations evidence, real-production approval, or command policy authority from IdP claims/UI/request bodies/assignment roles changed.
- NW-090 is accepted in commit `025baeb` after route commit `c432e84`: assignment administration now runs as server-rendered Spring MVC/Thymeleaf/HTML under production `/web-admin/assignments`, behind a valid web-admin session plus `web_admin.access`. Create/end mutations use the existing `AssignmentService` with the server-resolved session actor as command actor, existing `assignment_admin.create` / `assignment_admin.end` command capability, and same-assignment containment; only the session actor's active assignments are displayed. Backend review was GREEN. Verification: escalated `./mvnw -Dtest=WebAdminAssignmentWorkflowIntegrationTest,AssignmentContainmentIntegrationTest,AdminCommandCapabilityServiceIntegrationTest,WebAdminSessionBoundaryTest,ProductionAuthIntegrationTest,ProductionDevelopmentSurfaceFilterTest,WebAdminSecurityFoundationTest test` passed 65 tests, 0 failures, 0 errors, 0 skipped before the final test-only closure; final test-only closure added unauthenticated create/end POST coverage and escalated `./mvnw -Dtest=WebAdminAssignmentWorkflowIntegrationTest test` passed 9 tests, 0 failures, 0 errors; `git diff --cached --check` passed before commit. No Angular/SPA, `/admin` productionization, new command/scope authority, browser editing of assignment/admin command or principal-binding policy, contracts/schemas/sync/mobile/tenant internals, operations evidence, or real-production approval changed.
- NW-071 is accepted: shared-device session and local-state behavior now has durable platform authority at `docs/specifications/platform/shared-device-session-and-local-state.md`. The spec records current single-active-actor sessions, drain-or-seal switching, actor-resolved refresh/resume, per-actor mutable local partitions, actor-scoped sync bookkeeping, immutable shared config blobs, sealed pending-work boundaries, product-safe local-state vocabulary, non-goals, and escalation triggers. No runtime code, mobile login/UX polish, tenant-aware runtime/storage/sync partition changes, contract/schema/sync protocol behavior, retention/security promises beyond current actor partitioning, BAR/CDL/operations evidence, old IDR text, phase files, or real-production approval changed. NW-054 remains the route for expiry, decommissioning, sealed recovery, local encryption, token/session retention, no-local-retention, erasure, redaction, or sensitivity claims.
- NW-085 is accepted: external field-user Product Candidate 1 mobile login and token lifecycle now has durable platform authority at `docs/specifications/platform/mobile-oidc-login-and-token-lifecycle.md`. The selected login shape is mobile system browser/external-user-agent OIDC authorization-code with PKCE, with optional OS/provider native-app broker handoff when available, followed by `/api/auth/me` server actor resolution through explicit active `(issuer, subject) -> actor_id` binding. The spec records rejected alternatives, provider/non-production boundaries, refresh/logout/re-login/expiry expectations, token-placement limits, offline behavior, shared-device login UX vocabulary, successor implementation criteria, guard tests, non-goals, and escalation routes. No runtime code, mobile UI, token-storage implementation, tests, contracts, schemas, BAR, CDL, operations evidence, old IDR text, phase files, tenant-aware internals, retention/security policy, or real-production approval changed. NW-093 remains blocked for provider/region/jurisdiction, real users/data, support, compliance/security, and go/no-go approval; NW-054 remains the route for retention/security claims.
- NW-101 is accepted: commit `8dddff0` added the mobile external-user-agent OIDC + PKCE handoff, `/api/auth/me` actor activation, refresh/logout/re-login/expiry handling, switch-user/sign-out UI, actor-local session metadata, and Android callback bridge. Focused and full Flutter tests pass; `flutter analyze` still reports only pre-existing unrelated repo issues; and the native Android compile gate now passes with `timeout 900s ./gradlew :app:compileDebugKotlin --console=plain --no-daemon --stacktrace` from `mobile/android` in 3m 2s, including successful `:app:compileDebugKotlin`. The originally documented `mobile/./gradlew` path is absent; the wrapper lives under `mobile/android/gradlew`.
- NW-102 is accepted: implementation/control-surface commit `2d9e51e` added the compact status control panel, backlog Active Work Index, gap register maintenance rules, stale gap-row reconciliation for GAP-OPS-01/GAP-PRODUCT-01/GAP-CONFIG-02, artifact trace metadata, and acceptance evidence wording in the commit workflow. Verification: `git diff --check` passed, and targeted greps found the active control panel, Active Work Index, Gap Register Maintenance Rules, artifact trace table, and GAP-OPS-01 row. Gap register touched: yes. Artifact trace touched: yes. Active control panel updated: yes. No runtime code, contracts, schemas, specs, architecture decisions, product/platform behavior, BAR, CDL, or operations evidence changed.
- NW-103 is accepted: implementation/control-surface commit `d8c989d` added gap progress metadata inside the existing gap register/control plane, kept the five classifications unchanged, and populated initial metadata for current stall-risk rows. Verification: `git diff --check` passed, and targeted greps found Gap Progress Metadata, Evidence gate, and NW-103 status/backlog traces. Gap register touched: yes. Artifact trace touched: no. Active control panel updated: yes. No side document, new taxonomy, runtime code, contracts, schemas, specs, architecture decisions, product/platform behavior, BAR, CDL, artifact trace, or operations evidence changed.
- NW-104 is accepted: the three independent post-audit reports were copied into `docs/agent-working-surface/artifacts/` and indexed as non-authoritative evidence: `2026-06-19-product-goal-pm-handoff-audit.md`, `2026-06-19-test-ci-validation-strategy-audit.md`, and `2026-06-19-agents-md-instruction-file-audit.md`. Product implementation is frozen until the reset rows identified by these audits are accepted or explicitly parked. This did not rewrite AGENTS.md, create a PM handoff, add CI, or change runtime code, contracts, product scope, platform specs, architecture decisions, BAR, or CDL.
- NW-105 is accepted: root `AGENTS.md` is now a 77-line product-slice-first implementer router with nested `server/AGENTS.md`, `mobile/AGENTS.md`, and `contracts/AGENTS.md` files plus `docs/agent-working-surface/steward-session-guide.md` for broad steward workflows. Verification: `git diff --check` passed, `wc -l AGENTS.md` returned 77, and targeted greps passed. No runtime code, contract semantics, product scope, platform specs, architecture authority, PM handoff, validation matrix, skills/playbooks, mobile CI, BAR, CDL, gap register, or artifact trace changed.
- NW-106 is accepted: `docs/specifications/product/product-candidate-1-pm-handoff.md` is the derived PC1 PM planning surface and `docs/specifications/product/product-candidate-handoff-template.md` is the reusable future-PC handoff template. The handoff records the audit-supported product goal, target deployment boundary, primary users/jobs, in-scope journeys, explicit non-goals, current PC1 standing, scenario-to-PC1 slice map, candidate NW decomposition routes, product-level DoD, owner decisions, and next-product-NW selection rules. Verification: `git status --short`, `git diff --check`, required file checks, required greps, README indexing greps, backlog/status greps, and `wc -l` passed. Gap register touched: no. Artifact trace touched: no. Active control panel updated: yes. No runtime code, contracts, product scope, platform specs, architecture authority, validation matrix/policy, CI, skills/playbooks, mobile CI, PM successor acceptance, real-production approval, BAR, CDL, AGENTS files, or steward guide changed. At NW-106 acceptance, product implementation remained frozen until NW-107 through NW-109 were accepted or explicitly parked.
- NW-107 is accepted: `docs/agent-working-surface/validation-matrix.md` is the active validation-control surface for touched-surface gates and acceptance evidence format. It records evidence fields, validation levels, required validation by touched surface, command references, current gate standing, acceptance rules, PC1 smoke-gate standing, and immediate candidate-only follow-up routes. Verification: `git status --short`, `git diff --check`, file existence check, targeted greps, pointer greps, backlog/status greps, and `wc -l` passed; validation matrix line count is 151. Gap register touched: no. Artifact trace touched: no. Active control panel updated: yes. Runtime/code/contracts/product behavior changed: no. Product scope changed: no. Architecture authority changed: no. CI/test implementation changed: no. At NW-107 acceptance, product implementation remained frozen until NW-108 and NW-109 were accepted or explicitly parked.
- NW-108 is accepted: `docs/agent-working-surface/skills/README.md` is the active durable role-playbook index for PM Product Planner, Tester / Validation Auditor, Software Architect Boundary Reviewer, Implementation Agent, Reviewer, and Steward Session Guide routing. Added concise canonical role files under `docs/agent-working-surface/skills/`, Codex-discoverable thin wrappers under `.agents/skills/`, and working-surface README pointers for both. The playbooks are role guidance, not authority. Validation: `git status --short`, `git diff --check`, file existence checks, targeted greps, and `wc -l` passed. Gap register touched: no. Artifact trace touched: no. Active control panel updated: yes. Runtime/code/contracts/product behavior changed: no. Product scope changed: no. Architecture authority changed: no. Validation matrix changed: no. CI/test implementation changed: no. Product implementation remains frozen until NW-109 is accepted or explicitly parked.
- NW-109 is accepted: `.github/workflows/mobile-ci.yml` now runs `flutter test` and Android `:app:compileDebugKotlin` for matching mobile/contracts/workflow paths and manual dispatch. `docs/agent-working-surface/mobile-analyzer-known-issues.md` records `flutter analyze` as known-red with 7 current issues, not a blocking CI gate. `docs/agent-working-surface/validation-matrix.md` and `mobile/AGENTS.md` now route mobile full tests and Android compile as CI-backed while preserving analyzer cleanup/baseline as a future route. Verification: `git diff --check`, workflow file checks, targeted greps, `flutter pub get`, `flutter test` passed 144 tests, `flutter analyze` known-red with 7 issues, and Android compile passed in 1m34s. GitHub Actions Mobile CI run `27854302523` passed on PR head `e3f7cf2`; `flutter-test` and `android-compile` both succeeded. Gap register touched: no. Artifact trace touched: no. Active control panel updated: yes. Runtime/product behavior changed: no. Product scope changed: no. Architecture authority changed: no. Contracts changed: no. Validation matrix changed: yes. CI/test implementation changed: yes. Product implementation is held only for the post-audit reset checkpoint, not because reset rows remain incomplete.
- NW-110 is accepted: `docs/agent-working-surface/artifacts/NW-110-pc1-product-journey-smoke-definition.md` defines one bounded, domain-neutral PC1 setup-to-sync smoke path for PM/reviewer evaluation. It covers organization entry, web-admin login, setup draft/validate/readiness/approve/publish, assignment, mobile login/get-work/readiness/capture/local-sync/correction/sync, and narrow latest-synced/freshness/attention wording. Verification: docs-only `git diff --check`, artifact file/section checks, artifact trace grep, and status/backlog greps passed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime/product behavior changed: no. Product scope changed: no. Validation policy changed: no. Real-production approval changed: no. Domain-specific product direction changed: no. Successor candidates remain candidate-only until separately selected.
- NW-111 is accepted: `docs/agent-working-surface/artifacts/NW-111-pc1-synthetic-demo-walkthrough.md` turns the NW-110 smoke definition into a step-by-step, domain-neutral PC1 synthetic demo walkthrough for PM/reviewer comprehension. It reuses the neutral fixture, maps the setup-to-sync sequence to accepted PC1 support, defines pass criteria, and captures likely successor rows for setup/config polish, mobile polish, assignment proof polish, minimal freshness/attention view, vocabulary validation, or later smoke automation. Verification: docs-only `git diff --check`, artifact file/section checks, artifact trace grep, and status/backlog greps passed. Runtime tests skipped because no runtime code, tests, contracts, CI behavior, validation policy, or product scope changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no. Product behavior acceptance changed: no. Real-production approval changed: no. Reporting/export, retention/security, entity lifecycle, conflict automation, tenant/control-plane, contract/schema/sync behavior, BAR, CDL, and gap-register standing changed: no. Successor candidates remain candidate-only until separately selected.
- NW-112 is accepted: `docs/agent-working-surface/artifacts/NW-112-pc1-demo-review-and-successor-selection.md` reviews the NW-111 demo script step by step and selects exactly one successor. Sequences 1-15 are `CLEAR`; sequence 16 is `FRICTION` because the supervisor/reviewer latest-synced/freshness/attention beat needs a bounded non-reporting proof boundary before implementation. NW-113 is ready at `docs/agent-working-surface/prompts/NW-113-define-minimal-scoped-operational-freshness-attention-view.md` to define that minimal scoped operational view as product-validation/product-planning work only. Verification: docs-only `git diff --check`, artifact file checks, artifact trace grep, status/backlog greps, and successor prompt checks passed. Runtime tests skipped because no runtime code, tests, contracts, CI behavior, validation policy, or product scope changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no. Product behavior acceptance changed: no. Real-production approval changed: no. Reporting/export, retention/security, entity lifecycle, conflict automation, tenant/control-plane, contract/schema/sync behavior, BAR, CDL, and gap-register standing changed: no.
- NW-113 is accepted: `docs/agent-working-surface/artifacts/NW-113-minimal-scoped-operational-freshness-attention-view.md` defines the smallest PC1 supervisor/reviewer final-beat observation as latest synced work, product-safe freshness wording, and one read-only `Needs review` cue if present. The selected boundary is a separate implementation successor, NW-114, because current accepted standing is not enough for the synthetic demo final beat and deferring the beat would leave the only NW-112 friction unresolved. NW-114 is ready at `docs/agent-working-surface/prompts/NW-114-implement-minimal-operational-freshness-attention-view.md` and must stay inside a minimal scoped operational view, not reporting/export, broad audit, conflict operations, retention/security, entity lifecycle, tenant/control-plane work, contracts, schemas, sync changes, product-spec changes, or runtime behavior beyond the narrow view. Verification: docs-only `git diff --check`, artifact file checks, artifact trace grep, status/backlog greps, and successor prompt checks passed. Runtime tests skipped because no runtime code, tests, contracts, CI behavior, validation policy, or product scope changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no. Product behavior acceptance changed: no. Real-production approval changed: no. Reporting/export, retention/security, entity lifecycle, conflict automation, tenant/control-plane, contract/schema/sync behavior, BAR, CDL, and gap-register standing changed: no.
- NW-114 is accepted after review-fix commit `bb7ae6d`: `/web-admin/operational` now provides the minimal PC1 supervisor/reviewer observation as a server-rendered, read-only web-admin view behind an active web-admin session plus `web_admin.access` and `web_admin.read_scoped`. It shows only the latest visible synced work selected by applying active assignment-scope constraints before final ordering/limit, uses product-safe freshness wording from the server received/visibility time (`events.received_at`) while still saying this does not prove all devices are current, and shows at most one generic `Needs review` cue for unresolved attention attached to that work. The focused regression covers more than 200 newer out-of-scope work events plus one older in-scope event and still shows the in-scope event. Verification: `docker compose -f docker-compose.test.yml up -d test-db` passed; `git status --short` recorded the scoped server/docs changes; `git diff --check` passed; `./mvnw -Dtest=WebAdminOperationalViewIntegrationTest test` passed 8 tests, 0 failures, 0 errors, 0 skipped in 40.235 s; `./mvnw test` passed 392 tests, 0 failures, 0 errors, 0 skipped in 02:12 min. Stop condition fired: no. Gap register touched: no. Artifact trace touched: no. Active control panel updated: yes. Runtime implementation changed: yes. Product specs, platform specs, contracts, schemas, envelopes, sync protocol, migrations, validation matrix, CI, BAR, CDL, gap register, mobile code, reporting/export/import/warehouse/analytics, broad audit, conflict workflows, retention/security/offboarding promises, entity lifecycle/registry flows, tenant/control-plane behavior, and tenant/workspace selectors changed: no.
- NW-115 is accepted: `docs/agent-working-surface/artifacts/NW-115-pc1-post-nw114-demo-standing-and-successor-selection.md` reviews the 16 NW-111 synthetic demo sequences after accepted NW-114. All 16 sequences are `CLEAR`; sequence 16 is now clear because NW-114 provides the minimal scoped operational latest-synced/freshness/attention observation. PC1 is now `synthetic-demo-ready, not real-production-ready`. NW-116 is ready at `docs/agent-working-surface/prompts/NW-116-decide-pc1-proof-target-after-synthetic-demo-readiness.md` to choose the next proof target: internal synthetic demo, managed lab proof, real-use preparation through NW-093, or explicit park. Freshness boundary: NW-114 operational freshness is closed for PC1 demo proof; `GAP-PROJECTION-02` reporting freshness remains open for reporting/completeness/drilldown; NW-044 is not selected unless reporting pressure is selected. Verification: docs-only `git diff --check`, `rg "NW-115" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, artifact file check, and NW-116 prompt file check passed. Runtime tests skipped because no runtime code, tests, contracts, CI behavior, validation policy, or product scope changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no. Product behavior acceptance changed: no. Real-production approval changed: no. Reporting/export, retention/security, entity lifecycle, conflict automation, tenant/control-plane, contract/schema/sync behavior, BAR, CDL, validation matrix, CI, gap-register standing, mobile code, and server/web-admin implementation changed: no.
- NW-116 is accepted: `docs/agent-working-surface/artifacts/NW-116-pc1-proof-target-decision.md` selects internal synthetic demo as the next PC1 proof target after NW-115 marked PC1 `synthetic-demo-ready, not real-production-ready`. Managed lab proof is not selected because no lab organization, environment owner, support path, data boundary, or acceptance criteria are selected. Real-use preparation is not selected because NW-093 remains blocked without concrete real users/data, provider, region, jurisdiction, support, compliance/security, or go/no-go trigger. Explicit park is not selected because the cleared synthetic demo can now produce owner-review evidence. NW-117 is ready at `docs/agent-working-surface/prompts/NW-117-run-pc1-internal-synthetic-demo-proof.md` to capture the 16-sequence internal synthetic demo proof packet. Verification: docs-only `git diff --check`, `rg "NW-116" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, artifact file check, and grep for `synthetic-demo-ready, not real-production-ready` passed. Runtime tests skipped because no runtime code, tests, contracts, CI behavior, validation policy, or product scope changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no. Product behavior acceptance changed: no. Real-production approval changed: no. Reporting/export, retention/security, entity lifecycle, conflict automation, tenant/control-plane, contract/schema/sync behavior, BAR, CDL, validation matrix, CI, gap-register standing, mobile code, and server/web-admin implementation changed: no.
- NW-117 is accepted: `docs/agent-working-surface/artifacts/NW-117-pc1-internal-synthetic-demo-proof.md` records the internal synthetic demo proof selected by NW-116. All 16 NW-111 sequences are `PASS` using only the synthetic/non-sensitive `Example Organization` fixture, with no `FRICTION`, `NOT_RUN`, or `OUT-OF-SCOPE` sequences. PC1 remains `synthetic-demo-ready, not real-production-ready`; no real users, real organizational data, managed lab run, or real-production approval were used or granted. NW-117 selects managed lab proof boundary planning as the next owner route, not real-use preparation, repeat demo, or park. NW-118 is ready at `docs/agent-working-surface/prompts/NW-118-decide-pc1-managed-lab-proof-boundary.md` to define lab organization label, environment owner, operator/contact path, data boundary, fixture/data source, acceptance criteria, evidence owner, and stop conditions before any managed lab proof run. Verification: docs-only `git diff --check`, `rg "NW-117" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, artifact file check, and grep for `synthetic-demo-ready, not real-production-ready` passed. Runtime tests skipped because no runtime code, tests, contracts, CI behavior, validation policy, or product scope changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no. Product behavior acceptance changed: no. Real-production approval changed: no. Reporting/export, retention/security, entity lifecycle, conflict automation, tenant/control-plane, contract/schema/sync behavior, BAR, CDL, validation matrix, CI, gap-register standing, mobile code, and server/web-admin implementation changed: no.
- NW-118 is accepted: `docs/agent-working-surface/artifacts/NW-118-pc1-managed-lab-proof-boundary.md` selects a synthetic/non-sensitive managed lab proof as the next PC1 owner-review route. The lab boundary is `Example Lab Organization`, Datarun deployment owner as environment owner, owner-review lab operator/contact path, NW-111/NW-117 synthetic fixture data only, 16-sequence evidence acceptance criteria, product-validation evidence owner, and explicit stop conditions before real users/data, production approval, reporting/export, retention/security, conflict workflow, entity lifecycle, tenant/control-plane, contract/schema/sync, validation-policy/CI, BAR/CDL/gap-register, or runtime work. PC1 remains `synthetic-demo-ready, not real-production-ready`. NW-118 rejects NW-093 real-use preparation because no concrete real users/data, provider, region, jurisdiction, support, compliance/security, continuity, or go/no-go trigger is active; it also rejects repeating the internal demo and parking. NW-119 is ready at `docs/agent-working-surface/prompts/NW-119-run-pc1-managed-lab-proof.md` to capture the managed lab proof evidence packet. Verification: docs-only `git diff --check`, `rg "NW-118" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, artifact file check, and grep for `synthetic-demo-ready, not real-production-ready` passed. Runtime tests skipped because no runtime code, tests, contracts, CI behavior, validation policy, or product scope changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no. Product behavior acceptance changed: no. Real-production approval changed: no. Managed lab run changed: no. Reporting/export, retention/security, entity lifecycle, conflict automation, tenant/control-plane, contract/schema/sync behavior, BAR, CDL, validation matrix, CI, gap-register standing, mobile code, and server/web-admin implementation changed: no.
- NW-119 is accepted: `docs/agent-working-surface/artifacts/NW-119-pc1-managed-lab-proof.md` records the synthetic managed lab proof selected by NW-118. Evidence source: docs-only owner-review evidence over the NW-118 lab boundary, NW-111 synthetic walkthrough, and NW-117 all-PASS internal synthetic proof. Lab label: `Example Lab Organization`; environment owner: Datarun deployment owner for the selected lab environment; fixture/data source: NW-111/NW-117 synthetic labels adapted to lab context. All 16 NW-111 sequences are `PASS`, with no `FRICTION`, `NOT_RUN`, or `OUT-OF-SCOPE` sequences. PC1 remains `synthetic-demo-ready, not real-production-ready`; no real users, real organizational data, customer data, production secrets, runtime lab command, real-production approval, provider/region/jurisdiction selection, support commitment, compliance/security approval, continuity approval, or go/no-go ownership was used or granted. NW-119 recommends exactly one next move: park. No successor prompt was added. Future work must be selected separately through the PM handoff/backlog; real users/data require NW-093 first. Verification: docs-only `git diff --check`, `rg "NW-119" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, artifact file check, and grep for `synthetic-demo-ready, not real-production-ready` passed. Runtime tests skipped because no runtime code, tests, contracts, CI behavior, validation policy, or product scope changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no. Product behavior acceptance changed: no. Real-production approval changed: no. Managed lab runtime run changed: no. Reporting/export, retention/security, entity lifecycle, conflict automation, tenant/control-plane, contract/schema/sync behavior, BAR, CDL, validation matrix, CI, gap-register standing, mobile code, and server/web-admin implementation changed: no.
- NW-120 is accepted: `docs/agent-working-surface/artifacts/NW-120-delivery-readiness-and-pc2-intake.md` classifies post-PC1 delivery readiness and PC2 intake concerns across product fronts, validation debt, security/Secure SDLC, reliability/ops, architecture risk, production approval, tenant/control-plane, code-boundary debt, and scenario follow-through. It confirms PC1 remains `synthetic-demo-ready, not real-production-ready`, keeps NW-093 blocked for real users/data, keeps NW-094 through NW-098 trigger-based, and recommends exactly one successor: NW-121 to select the PC2 product candidate boundary and PM handoff. NW-120 does not select PC2 implementation, accept product behavior, approve real production, change validation policy, or change runtime architecture. Validation: docs-only `git diff --check`, `rg "NW-120" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md docs/agent-working-surface/artifacts/README.md`, artifact file check, NW-121 prompt file check, and `rg "NW-121" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md` passed. Runtime tests skipped because no runtime code, tests, contracts, CI behavior, validation policy, or product scope changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no. Product behavior acceptance changed: no. Real-production approval changed: no. Reporting/export/import/warehouse/analytics acceptance, conflict workflow, retention/security promises, entity lifecycle, tenant/control-plane work, contracts, schemas, sync behavior, BAR, CDL, validation matrix, CI, gap-register changes, mobile code, and server/web-admin implementation changed: no.
- NW-121 is accepted: `docs/specifications/product/product-candidate-2-pm-handoff.md` selects exactly one PC2 planning boundary: Product Candidate 2 - Single Work-Linked Attention Review. The handoff explains why this boundary wins over setup/admin polish, assignment/admin polish, mobile polish, broad reporting/import/export, conflict operations, pattern follow-through, S06 lifecycle, and tenant/control-plane fronts. It identified the PC2-scoped NW-072 prerequisite, now accepted separately, as the required route for current conflict/flag durable behavior and the operational attention read-model/query boundary before implementation, including the `EventRepository`/`WebAdminOperationalViewService` reach-through guardrail. NW-121 does not accept product behavior, implement runtime code, approve production, change validation policy, mutate BAR/CDL/gap register, accept reporting/import/export behavior, accept conflict automation/batch behavior, promote S06, or select tenant/control-plane runtime work. Validation: docs-only `git diff --check`, `rg "NW-121" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, `test -f docs/specifications/product/product-candidate-2-pm-handoff.md`, and product-index grep passed. Runtime tests skipped because no runtime code, tests, contracts, schemas, CI behavior, validation policy, product behavior acceptance, BAR, CDL, gap-register standing, mobile code, or server/web-admin implementation changed. Gap register touched: no. Artifact trace touched: no. Product spec index touched: yes. Active control panel updated: yes.
- NW-072 is accepted: `docs/specifications/platform/conflict-flag-resolution-and-attention-query-boundary.md` extracts the PC2-scoped conflict/flag durable behavior and operational attention query boundary required by NW-121 before any Single Work-Linked Attention Review implementation. It records what current flag/resolution behavior is normative enough for PC2 product copy, represents authority as exact stored `designated_resolver` equality, states that resolver eligibility fallback is not product-safe as a general authority promise, and requires a bounded operational attention query/read-model boundary instead of extending `WebAdminOperationalViewService` direct `JdbcTemplate` reach-through. NW-072 does not implement PC2, add a review UI/API, accept reporting/import/export, accept conflict automation/batch/resolver reassignment, change schemas/contracts/sync, mutate BAR/CDL, add new gap IDs, change gap classifications, approve production, or use real users/data. The follow-up control-plane reconciliation updates only GAP-CONFLICT-01 through GAP-CONFLICT-03 routing so future work starts from accepted NW-072 rather than reselecting it. Validation: docs-only `git diff --check`, `git diff --cached --check`, `rg "last-reviewed: 2026-06-21" docs/agent-working-surface/decision-anchor-layer/gap-routing-playbook.md`, `rg "NW-072" docs/agent-working-surface/decision-anchor-layer/gap-routing-playbook.md docs/status.md`, and `rg "resolver fallback" docs/agent-working-surface/decision-anchor-layer/gap-routing-playbook.md` passed. Runtime tests skipped because no runtime code, tests, contracts, schemas, CI behavior, validation policy, product behavior acceptance, BAR, CDL, mobile code, server/web-admin implementation, reporting/import/export behavior, conflict automation, batch behavior, resolver reassignment, tenant/control-plane, real-production approval, or real users/data changed. Gap register touched: yes, conflict gap routing reconciled after accepted NW-072. Artifact trace touched: no. Product handoff touched: yes. Platform spec index touched: yes. Active control panel updated: yes.
- NW-122 is accepted: `/web-admin/operational` now links its single visible `Needs review` cue to one server-rendered attention review page for the current scoped work item. The implementation uses a typed bounded `EventRepository` operational attention read model instead of adding another direct attention SQL reach-through in `WebAdminOperationalViewService`; re-applies accepted assignment scope before returning attention details; treats a flag as unresolved only when no canonical exact-resolver `conflict_resolved/v1` exists; and uses the web-admin session actor only. Manual resolution binds the POST to the opened attention item with an opaque session token, re-queries that specific unresolved visible flag, requires exact stored `designated_resolver` equality to the session actor, calls existing append-only `ConflictResolutionService.resolve(...)`, ignores body/UI actor spoofing, and blocks `resolver_unassigned` without fallback, override, or reassignment. Raw actor UUIDs and raw detector reasons are not rendered in the PC2 product UI; detector reasons remain stored on events and are mapped to bounded product copy for display. The UI stays server-rendered and one-item only, with product-safe resolver and attention wording and no queue, list, batch workflow, filter, export/import, reporting, broad conflict console, schema/contract/sync, tenant/control-plane, production approval, or real users/data changes. GAP-CONFLICT-01 is reconciled so the accepted bounded one-item UI no longer routes as open implementation; remaining conflict gap work is future queue/multi-item ergonomics only if separately selected. Validation: `docker compose -f docker-compose.test.yml up -d test-db` passed; `git diff --check` passed; focused `./mvnw -Dtest=WebAdminOperationalViewIntegrationTest,ConflictResolutionIntegrationTest test` passed 32 tests, 0 failures, 0 errors, 0 skipped in 41.694 s; full server `./mvnw test` passed 401 tests, 0 failures, 0 errors, 0 skipped in 01:22 min. Stop condition fired: no. Gap register touched: yes. Artifact trace touched: no. Active control panel updated: yes. Runtime implementation changed: yes.
- NW-123 is accepted: `docs/agent-working-surface/artifacts/NW-123-pc2-post-nw122-demo-standing-and-successor-selection.md` reviews PC2 after accepted NW-122 / merged PR #38. It finds that NW-122 satisfies Single Work-Linked Attention Review for synthetic proof, marks PC2 `synthetic-demo-ready, not proof-complete, not real-production-ready`, finds no required small polish before proof, and selects exactly one successor: NW-124 PC2 synthetic walkthrough/proof packet. Concerns are routed to existing rows/gaps instead of buried: real users/data through NW-093; reporting/import/export through NW-044 and projection gaps; queue/list/multi-item ergonomics through GAP-CONFLICT-01; automation/batch/resolver reassignment through NW-045; resolver eligibility broadening through GAP-CONFLICT-03; and tenant/control-plane work through NW-094 through NW-098. Validation: docs-only `git diff --check`, `rg "NW-123" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, and artifact file check passed. Runtime tests skipped because no runtime code, tests, contracts, schemas, CI behavior, validation policy, product spec, platform spec, BAR, CDL, gap-register standing, mobile code, or server/web-admin implementation changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no.
- NW-124 is accepted: `docs/agent-working-surface/artifacts/NW-124-pc2-synthetic-walkthrough-proof.md` records PC2 owner-review proof evidence after accepted NW-122 / PR #38 and NW-123. It walks the one-item `Needs review` cue, one attention review page, exact designated-reviewer authority, append-only source-work preservation, and one manual decision from accepted implementation/test evidence. Beat classifications are `PASS` for the evidence-supported PC2 review loop, `NOT_RUN` for live browser/manual runtime inspection, and `OUT-OF-SCOPE` for list/queue/reporting/batch/automation/reassignment/eligibility-broadening paths. PC2 remains `synthetic-demo-ready, not real-production-ready`; no real users/data, production approval, reporting/import/export, queue/list/multi-item review, automation/batch/resolver reassignment, resolver eligibility broadening, or tenant/control-plane work is selected. NW-124 recommends exactly one next move: park PC2 proof route. No successor prompt was added. Future PC2 work must be selected separately; real users/data require NW-093 first. Validation: docs-only `git diff --check`, `rg "NW-124" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, artifact file check, and grep for `synthetic-demo-ready` passed. Runtime tests skipped because no runtime code, tests, contracts, schemas, CI behavior, validation policy, product spec, platform spec, BAR, CDL, gap-register standing, mobile code, or server/web-admin implementation changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no.
- NW-125 is accepted: `docs/agent-working-surface/artifacts/NW-125-pc2-synthetic-lab-proof-environment.md` records bounded PC2 lab environment-preparation evidence and classifies the PC2 lab proof environment as `NOT_READY`. R12 was inspected first and remains the accepted synthetic reference deployment evidence target, not a PC2 proof target: its app image source revision `757d6c8d386f760693157c3e1388c877efdf6a0e` predates PC2 and `/web-admin/operational` returns `404`. NW-125 started an isolated PC2 prep attempt on `datarun-app.lab` from current `main` commit `9dee62d8019d13076ce359db3be999d4db916200` using separate source path `/home/nmcp/datarun-platform-pc2-src`, compose project `datarun-pc2-nw125`, DB port `127.0.0.1:25432`, and app port `127.0.0.1:28080`, but Docker build/run output was truncated and subsequent hostname/direct-IP SSH access to the lab failed before PC2 container state, `/web-admin/operational` reachability, synthetic web-admin session/provisioning, cleanup state, or post-prep R12 continuity could be verified. No live browser proof, real users/data, production approval, runtime code, Dockerfile/build-tooling, tests, schemas, CI, product/platform specs, BAR, CDL, gap-register changes, auth bypass, reporting/import/export, queue/list/multi-item review, automation/batch/reassignment, resolver eligibility broadening, or tenant/control-plane work was selected. Runtime tests were skipped because no runtime code changed; docs validation passed.
- NW-126 is accepted: `docs/agent-working-surface/artifacts/NW-126-pc2-synthetic-lab-environment-reconciliation.md` records the unblocked PC2 lab reconciliation from current `main` after PR #54 merge. R12 was inspected first and remained unchanged: `datarun-reference-server-1` stayed healthy on image digest `sha256:5f246547e1292092b133540a86efe230fd9f9bacc1f73bccc83e8644e4fb82e2`, source revision `757d6c8d386f760693157c3e1388c877efdf6a0e`, loopback ports `18080/18081`, `/api/auth/me` 401 without token, and `/web-admin/operational` 404. Retained NW-125 state was reconciled at `/home/nmcp/datarun-platform-pc2-src`; the isolated PC2 source was fast-forwarded to `75880aafd346d06d4439b037b29d0d193a02f7ec`, image `datarun-pc2-nw125-server:latest` built successfully, and compose project `datarun-pc2-nw125` now runs with loopback-only ports `127.0.0.1:25432` and `127.0.0.1:28080`. PC2 health is UP, `/api/auth/me` is 401 without token, and `/web-admin/operational` redirects to `/web-admin/login`, so the route exists. The environment is still `NOT_READY` for live browser proof because no synthetic web-admin OIDC/session/provisioning path exists and `/web-admin/login` returns 500 with `oidc_issuer_not_configured`. No live browser proof, R12 mutation, real users/data, production approval, runtime code, Dockerfile/build-tooling, tests, schemas, CI, product/platform specs, BAR, CDL, gap-register changes, auth bypass, reporting/import/export, queue/list/multi-item review, automation/batch/reassignment, resolver eligibility broadening, tenant/control-plane work, mobile code, or server/web-admin implementation changed. Exactly one successor route is selected: NW-139 PC2 synthetic web-admin session provisioning.
- NW-139 is accepted: `docs/agent-working-surface/artifacts/NW-139-pc2-synthetic-web-admin-session-provisioning.md` records the isolated PC2 synthetic web-admin OIDC/session/provisioning path as `READY`. R12 was inspected before and after and remained unchanged, including R12 app health, DB1 recovery/backup evidence, monitoring evidence, token cleanup standing, and Keycloak standing. The retained isolated PC2 stack now includes a synthetic OIDC provider under `/home/nmcp/datarun-pc2-nw139`, bound to `172.17.0.1:28090`, and the server compose config includes `/home/nmcp/datarun-pc2-nw139/compose.oidc.override.yml`. Accepted one-shot provisioning commands applied one active binding for issuer `http://127.0.0.1:28090`, subject `pc2-synthetic-web-admin`, actor `33333333-3333-4333-8333-333333333333`, and published reviewed config version 1 with exactly `web_admin.access` and `web_admin.read_scoped` for that actor. `/web-admin/login` now returns HTTP 302 to the configured synthetic OIDC flow, HTTP-only callback validation returned HTTP 303 to `/web-admin/shell`, and authenticated `/web-admin/shell` and `/web-admin/operational` returned HTTP 200. Transient session-cookie and authorization-code evidence was cleaned. No live browser proof, R12 mutation, R12 Keycloak mutation or repurposing, real users/data/secrets, production approval, auth bypass/dev-login shortcut, runtime code, Dockerfile/build-tooling, schemas, tests, CI, product/platform specs, BAR, CDL, gap-register changes, reporting/export, queue/list review, automation, tenant/control-plane, mobile code, server feature implementation, or all-PC proof occurred. Exactly one successor route is selected: NW-140 PC2 live browser walkthrough proof.
- NW-140 is accepted: `docs/agent-working-surface/artifacts/NW-140-pc2-live-browser-walkthrough-proof.md` records the PC2 live browser walkthrough proof attempt as `NOT_READY`. R12 was inspected before and after PC2 access and remained unchanged: the R12 app container stayed on image digest `sha256:5f246547e1292092b133540a86efe230fd9f9bacc1f73bccc83e8644e4fb82e2`, source revision `757d6c8d386f760693157c3e1388c877efdf6a0e`, loopback ports `18080/18081`, readiness/liveness HTTP 200 on management port `18081`, `/api/auth/me` 401 without token, and `/web-admin/operational` 404; R12 evidence/config roots, DB1 evidence root, Keycloak, and Alertmanager remained present/up. The retained PC2 stack still matched NW-139: source revision `75880aafd346d06d4439b037b29d0d193a02f7ec`, compose project `datarun-pc2-nw125`, app port `127.0.0.1:28080`, synthetic OIDC provider `172.17.0.1:28090`, active binding for issuer `http://127.0.0.1:28090` / subject `pc2-synthetic-web-admin` / actor `33333333-3333-4333-8333-333333333333`, and reviewed config with exactly `web_admin.access` and `web_admin.read_scoped`. The two-port tunnel reached app readiness and OIDC discovery, `/web-admin/operational` redirected through the synthetic OIDC flow, a `JSESSIONID` was created, and authenticated `/web-admin/operational` returned HTTP 200. The page showed no visible scoped work, no `Needs review` cue, no attention link, and no resolution form, so the one-item attention-review walkthrough could not proceed. The current operator environment also lacked a usable browser driver because `firefox` is a snap stub and Playwright/Puppeteer/Selenium are absent. Runtime automated tests skipped because no runtime code changed; docs validation passed. Artifact trace touched: yes. Gap register touched: no. Active control panel updated: yes. No R12 mutation, R12 Keycloak mutation/repurposing, real users/data/secrets, production approval, auth bypass/dev-login, runtime app code, Dockerfile/build-tooling, schemas, tests, CI, product/platform specs, BAR, CDL, gap-register changes, reporting/export, queue/list review, automation, tenant/control-plane, mobile code, server feature implementation, or all-PC proof occurred. Exactly one successor selected: NW-141 PC2 live walkthrough fixture and browser path.
- NW-141 is accepted: `docs/agent-working-surface/artifacts/NW-141-pc2-live-walkthrough-preconditions.md` records PC2 live walkthrough preconditions as `READY`. R12 continuity was inspected before and after PC2 fixture/browser work and remained unchanged: the R12 app container stayed healthy on image digest `sha256:5f246547e1292092b133540a86efe230fd9f9bacc1f73bccc83e8644e4fb82e2`, source revision `757d6c8d386f760693157c3e1388c877efdf6a0e`, loopback ports `18080/18081`, readiness HTTP 200, `/api/auth/me` 401 without token, and `/web-admin/operational` 404; R12 evidence/config roots, DB1 evidence root, Keycloak, and Alertmanager remained present/up. The retained isolated PC2 stack still matched NW-139 and now has exactly one scoped synthetic source work event for subject `14114114-2141-4141-9141-141141141141` / activity `field_visit`, exactly one unresolved work-linked attention item designated to synthetic actor `33333333-3333-4333-8333-333333333333`, and authenticated `/web-admin/operational` shows the scoped work, `Needs review`, and the attention review link. A generated extra detector flag was resolved through the accepted bearer-bound conflict API by the exact synthetic actor, leaving one unresolved fixture attention item. Headless Chromium browser evidence through the required two-port tunnel passed: synthetic OIDC login/session worked, `/web-admin/operational` showed the scoped work and cue, `/web-admin/operational/attention` opened, resolver standing matched the synthetic actor, and the resolution form was present. Runtime automated tests skipped because no runtime code changed; docs validation passed. Artifact trace touched: yes. Gap register touched: no. Active control panel updated: yes. No R12 mutation, R12 Keycloak mutation/repurposing, real users/data/secrets, production approval, auth bypass/dev-login, runtime app code, Dockerfile/build-tooling, schemas, tests, CI, product/platform specs, BAR, CDL, gap-register changes, reporting/export, queue/list review, automation, tenant/control-plane, mobile code, server feature implementation, or all-PC proof occurred. Exactly one successor selected: NW-142 PC2 live browser walkthrough proof.
- NW-142 is accepted as a stopped `NOT_READY` proof attempt: `docs/agent-working-surface/artifacts/NW-142-pc2-live-browser-walkthrough-proof.md` records that R12 continuity was inspected before and after PC2 preflight and remained unchanged. The retained isolated PC2 stack still matched NW-141 infrastructure/auth standing, but the required unresolved fixture was no longer intact: unresolved flag counts were `0`, and accepted resolution event `bcafd46a-d439-4548-b51d-bc8ec9dddb09` had already resolved fixture flag `14114114-4141-4141-9141-141141141141` with actor `33333333-3333-4333-8333-333333333333`, resolution `accepted`, and reason `ok, manual`. Per owner instruction, the agent stopped instead of creating a replacement fixture or improvising proof from a consumed item. No browser proof was run, no tunnel was established for the proof, and no NW-142 resolution was submitted by the agent. Runtime automated tests skipped because no runtime code changed; docs validation passed. Artifact trace touched: yes. Gap register touched: no. Active control panel updated: yes. No R12 mutation, R12 Keycloak mutation/repurposing, real users/data/secrets, production approval, auth bypass/dev-login, runtime app code, Dockerfile/build-tooling, schemas, tests, CI, product/platform specs, BAR, CDL, gap-register changes, reporting/export, queue/list review, automation, tenant/control-plane, mobile code, server feature implementation, or all-PC proof occurred. Exactly one successor selected: NW-143 consumed PC2 fixture reconciliation and proof retry-path selection.
- NW-127 is accepted: `docs/specifications/product/product-candidate-3-pm-handoff.md` selects exactly one PC3 planning boundary: Product Candidate 3 - Scoped Operational Report Snapshot. The handoff explains why this boundary wins over setup/admin polish, assignment/admin polish, mobile polish, broad reporting/import/export, conflict queue/list work, pattern follow-through, S06 lifecycle, tenant/control-plane, and delivery-readiness/security/reliability proof fronts. It selects exactly one successor route: NW-128 to specify the PC3 scoped operational report snapshot boundary before implementation. NW-127 keeps NW-093 blocked for real users/data, preserves NW-126 blocked PC2 live-lab proof debt, keeps NW-094 through NW-098 trigger-based, keeps NW-044 as the broad reporting/import/export route, keeps NW-073 unselected unless the boundary actually depends on pattern registry/projection behavior, and does not implement runtime code or accept product behavior. Validation: docs-only `git diff --check`, `rg "NW-127" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, `test -f docs/specifications/product/product-candidate-3-pm-handoff.md`, product-index grep, NW-128 prompt file check, and `rg "NW-128" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md` passed. Runtime tests skipped because no runtime code, tests, contracts, schemas, CI behavior, validation policy, product behavior acceptance, BAR, CDL, gap-register standing, mobile code, or server/web-admin implementation changed. Gap register touched: no. Artifact trace touched: no. Product spec index touched: yes. Active control panel updated: yes.
- NW-128 is accepted: `docs/specifications/platform/scoped-operational-report-snapshot-boundary.md` accepts the PC3 platform boundary as one current scoped operational standing snapshot with limited traceability. It selects `/web-admin/operational/report` as the first read-only server-rendered implementation target, requires web-admin session plus `web_admin.access` and `web_admin.read_scoped`, requires scope-before-aggregate/no-leakage behavior, defines evidence freshness without thresholded stale/overdue or completion claims, defines clean/excluded-unresolved/unresolved issue counts, limits trace links to existing scoped work or one accepted attention context, and requires a typed bounded read-model/query boundary. NW-128 decides no separate PC3 product behavior spec is needed now; the PM handoff plus this platform boundary are sufficient until the snapshot expands or product copy needs accepted period/cadence/owner-language commitments. Exactly one successor is ready: NW-129 to implement PC3 scoped operational report snapshot. NW-128 preserves NW-093 real-use gating, NW-126 blocked PC2 lab debt, NW-044 broad reporting/import/export route, NW-073 only-if-needed pattern route, NW-053 scope route, NW-054 retention/security route, and NW-094 through NW-098 tenant/control-plane routes. Validation: docs-only `git diff --check`, `rg "NW-128" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, platform spec file check, platform-index grep, NW-129 trace grep, and route-preservation grep passed. Runtime tests skipped because no runtime code, tests, contracts, schemas, migrations, CI behavior, validation policy, product behavior spec, BAR, CDL, gap-register standing, mobile code, server/web-admin implementation, reporting/import/export behavior, conflict automation, tenant/control-plane, real-production approval, real users/data, lab state, or PC2 live proof changed. Gap register touched: no. Artifact trace touched: no. Platform spec index touched: yes. Product handoff touched: yes. Active control panel updated: yes.
- NW-129 is accepted in implementation commits `6336d7e` and `8b4daaf`: `/web-admin/operational/report` now renders one read-only server-rendered PC3 Scoped Operational Report Snapshot behind a valid web-admin session, `web_admin.access`, and `web_admin.read_scoped`. The implementation adds a typed `ScopedOperationalReportSnapshotService` boundary and bounded event-query aggregation that applies accepted assignment/designated-resolver visibility before activity rows, counts, latest visible input, clean source-work times, unresolved issue counts, and trace context. It renders `snapshot_as_of`, `known_latest_input` / `no_visible_input` / `unknown_latest_input` states where reachable from accepted inputs, scoped-view caveats, clean/excluded-unresolved/unresolved counts, activity standing rows seeded from visible configured/assigned activities, and limited trace context through existing scoped operational context. Assigned/configured activities with no visible source work render zero-count rows with unknown latest times and `Coverage not measured`. It does not add public report APIs, exports/imports, report warehouse/catalog/dashboard behavior, arbitrary filters, migrations, contracts/schemas/sync changes, pattern projection work, tenant/control-plane behavior, PC2 live proof, lab mutation, or real users/data. Verification: `docker compose -f docker-compose.test.yml up -d test-db` confirmed the local test DB was running; `./mvnw -Dtest=WebAdminOperationalReportSnapshotIntegrationTest test` passed 12 tests, 0 failures, 0 errors, 0 skipped in 20.180 s; `./mvnw -Dtest=WebAdminOperationalViewIntegrationTest,WebAdminOperationalReportSnapshotIntegrationTest test` passed 29 tests, 0 failures, 0 errors, 0 skipped in 23.696 s; `./mvnw test` passed 413 tests, 0 failures, 0 errors, 0 skipped in 01:15 min; `git diff --check` passed. Gap register touched: no. Artifact trace touched: no. Active control panel updated: yes. Exactly one successor is selected: NW-130 to review the PC3 snapshot implementation and prepare the synthetic walkthrough/proof route; the execution packet is `docs/agent-working-surface/prompts/NW-130-review-pc3-snapshot-implementation-and-prepare-synthetic-proof-route.md`.
- NW-130 is accepted: `docs/agent-working-surface/artifacts/NW-130-pc3-post-nw129-snapshot-standing-and-successor-selection.md` reviews the accepted NW-129 `/web-admin/operational/report` implementation against the PC3 PM handoff and accepted NW-128 platform boundary. The review finds the snapshot sufficient for synthetic owner proof, marks PC3 `synthetic-demo-ready, not proof-complete, not real-production-ready`, finds no required small polish/fix before proof, and selects exactly one successor: NW-131 PC3 synthetic walkthrough/proof using S26 operational reporting as the primary example. The review explicitly covers access gates, scope-before-aggregate/no-leakage behavior, freshness states, completeness caveats, unresolved issue treatment, limited traceability, and typed read-model/query boundaries. Candidate-only follow-up pressure is limited to trace/copy polish if proof records concrete friction. NW-130 preserves NW-093 real-use gating, NW-126 blocked PC2 lab debt, NW-044 broad reporting/import/export route, NW-073 only-if-needed pattern route, NW-053 scope route, NW-054 retention/security route, and NW-094 through NW-098 tenant/control-plane routes. Validation: docs-only `git diff --check`, `rg "NW-130" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, artifact file check, NW-131 prompt file check, and `rg "NW-131" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md docs/agent-working-surface/prompts/NW-131-run-pc3-synthetic-walkthrough-proof.md` passed. Runtime tests skipped because no runtime code, tests, contracts, schemas, CI behavior, validation policy, product spec, platform spec, BAR, CDL, gap-register standing, mobile code, or server/web-admin implementation changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no.
- NW-131 is accepted: `docs/agent-working-surface/artifacts/NW-131-pc3-synthetic-walkthrough-proof.md` records one synthetic/non-sensitive PC3 owner-review proof over the accepted `/web-admin/operational/report` Scoped Operational Report Snapshot using S26 operational reporting and aggregate oversight as the example. The proof classifies evidence-supported beats as `PASS`, live browser/manual inspection as `NOT_RUN`, and broad reporting/import/export/warehouse/API/catalog/filter/cadence/percentage/completion/all-clear, queue/list/batch/automation/reassignment, pattern, new-scope, retention/security, tenant/control-plane, PC2 lab, real-user/data, runtime, contract/schema/sync/CI/BAR/CDL/gap-register work as `OUT-OF-SCOPE`. It confirms access gates, scoped/no-leakage standing, scope-before-aggregate ordering, freshness states, completeness caveats, unresolved issue treatment, limited traceability, and typed read-model/query boundaries from accepted NW-129 implementation/test evidence. No friction was recorded. PC3 remains `synthetic-demo-ready, not real-production-ready`; no real users/data or real-production approval were used or granted. NW-131 recommends exactly one next move: park PC3 proof route. No successor prompt was added. Validation: docs-only `git diff --check`, `rg "NW-131" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, artifact file check, and grep for `synthetic-demo-ready` passed. Runtime tests skipped because no runtime code, tests, contracts, schemas, CI behavior, validation policy, product spec, platform spec, BAR, CDL, gap-register standing, mobile code, or server/web-admin implementation changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no.
- NW-132 is accepted: `docs/agent-working-surface/artifacts/NW-132-post-pc3-progress-health-and-next-route-selection.md` reviews post-PC3 progress health after PC1, PC2, and PC3 synthetic proof routes were completed and parked. It confirms PC1-PC3 are synthetic-demo-ready only and not real-production-ready, finds no buried concerns, keeps NW-093 blocked for real users/data, keeps NW-126 blocked on lab access, preserves NW-044, NW-045, NW-053, NW-054, NW-073, NW-021, NW-036, and NW-094 through NW-098 as trigger-based routes, and finds the control plane healthy enough to select the next product candidate. It rejects engineering/validation hardening, Secure SDLC, reliability/ops readiness, architecture risk reduction, PC2 lab reconciliation, pattern extraction, tenant/control-plane work, broad reporting/import/export, and no-active-route as the next move because their triggers are not active. Exactly one successor is selected: NW-133 to select the PC4 product candidate boundary and PM handoff. Validation: docs-only `git diff --check`, `rg "NW-132" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, artifact file check, PC1/PC2/PC3 artifact greps, and `git diff --cached --check` passed. Runtime tests skipped because no runtime code, tests, contracts, schemas, migrations, CI behavior, validation policy, product/platform behavior, BAR, CDL, gap-register classifications, mobile code, server code, or web-admin implementation changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no.
- NW-133 is accepted: `docs/specifications/product/product-candidate-4-pm-handoff.md` selects exactly one PC4 boundary, Product Candidate 4 - Operational Responsibility Handoff, as a derived PM planning surface. The handoff compares remaining fronts after PC1, PC2, and PC3, rejects setup/admin polish, mobile polish, live proof, broad reporting/import/export, queue/list/batch/automation, pattern work, S06/entity lifecycle, retention/security/offboarding, tenant/control-plane, engineering hardening, Secure SDLC, reliability/ops, and real-production approval as PC4 because their triggers are not active. It preserves PC1, PC2, and PC3 as synthetic-demo-ready only and not real-production-ready, keeps NW-093 blocked for real users/data, keeps NW-126 blocked on lab access, preserves NW-044, NW-045, NW-053, NW-054, NW-073, NW-021, NW-036, and NW-094 through NW-098 as trigger-based routes, and selects exactly one successor: NW-134 to specify the PC4 operational responsibility handoff boundary before implementation. Validation: docs-only `git diff --check`, `rg "NW-133" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, product handoff file check, product-index grep, NW-134 prompt file check, and `rg "NW-134" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md` passed. Runtime tests skipped because no runtime code, tests, contracts, schemas, migrations, CI behavior, validation policy, product/platform behavior acceptance, BAR, CDL, gap-register classifications, mobile code, server code, web-admin implementation, lab state, real-production approval, or real users/data changed. Gap register touched: no. Artifact trace touched: no. Product spec index touched: yes. Active control panel updated: yes. Runtime implementation changed: no.
- NW-134 is accepted: `docs/specifications/platform/operational-responsibility-handoff-boundary.md` is the accepted prerequisite platform boundary for PC4 Operational Responsibility Handoff. The spec selects one boundary: a successor start packet for current assigned work, with bounded prior context and caveated late-work standing. It uses S25 worker transfer as the primary synthetic example, S27 logistics handoff as the domain-neutral cross-check, and S22 campaign reassignment only as secondary continuity evidence. It defines accepted authority inputs, assignment boundaries, successor-visible current work and prior context, late offline work treatment, subject-history/sync/projection/actor-local boundaries, stale/incomplete/unknown/unresolved caveats, no-leakage/no-broad-audit expectations, product-safe wording, security and reliability gates, preserved trigger routes, validation gates, and implementation readiness. It preserves PC1, PC2, and PC3 as synthetic-demo-ready only and not real-production-ready; keeps NW-093 blocked for real users/data; keeps NW-126 blocked on lab access; preserves NW-044, NW-045, NW-053, NW-054, NW-073, NW-021, NW-036, and NW-094 through NW-098 as trigger-based routes; and selects exactly one successor: NW-135 to implement the bounded PC4 handoff context. Validation: docs-only `git diff --check`, `rg "NW-134" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, platform spec file check, platform-index grep, NW-135 prompt file check, NW-135 trace grep, and route-preservation grep passed. Runtime tests skipped because no runtime code, tests, contracts, schemas, migrations, CI behavior, validation policy, product behavior spec, BAR, CDL, gap-register standing, mobile code, server/web-admin implementation, reporting/import/export behavior, conflict automation, pattern behavior, entity lifecycle, retention/security/offboarding policy, tenant/control-plane behavior, real-production approval, real users/data, lab state, or PC2 live proof changed. Platform spec index touched: yes. Product handoff touched: yes. Gap register touched: no. Artifact trace touched: no. Active control panel updated: yes. Runtime implementation changed: no.
- NW-135 is accepted: implemented one read-only server-rendered `/web-admin/operational/handoff` PC4 Operational Responsibility Handoff surface behind a valid web-admin session plus `web_admin.access` and `web_admin.read_scoped`. The implementation adds a typed `OperationalResponsibilityHandoffService` and bounded `EventRepository` queries that apply accepted assignment scope before current assigned work, prior context, caveats, latest visible input, trace target, and empty-state selection. It shows current assigned work and bounded prior context for visible subject/activity slices, reuses existing visible unresolved attention/designated-resolver standing, and renders product-safe caveats for incomplete context, unknown freshness, late synced/offline work, stale responsibility, needs-attention standing, and not-currently-resolvable resolver states without fallback authority. Hidden out-of-scope records do not affect counts, latest times, caveats, trace hints, or empty states, and the page has no mutation route. Validation: `git status --short` was clean at start, `git diff --check` passed before runtime tests, `docker compose -f docker-compose.test.yml up -d test-db` passed, initial non-escalated focused Maven failed before assertions due sandboxed local Postgres access, approved rerun of `./mvnw -Dtest=WebAdminOperationalHandoffIntegrationTest test` passed 6 tests, 0 failures, 0 errors, 0 skipped in 16.80 s, and approved full `./mvnw test` passed 419 tests, 0 failures, 0 errors, 0 skipped in 01:35 min. No broad reporting/import/export, broad audit/history, conflict queue/list/batch/automation, resolver reassignment/fallback authority, retention/security/offboarding promise, new scope, pattern projection/API, entity lifecycle, tenant/control-plane, lab mutation, PC2 live proof, real users/data, real-production approval, contracts, schemas, migrations, sync protocol, validation policy, BAR, CDL, gap-register, CI, or mobile change occurred. Gap register touched: no. Artifact trace touched: no. Active control panel updated: yes. Runtime implementation changed: yes.
- NW-136 is accepted: `docs/agent-working-surface/artifacts/NW-136-pc4-post-nw135-handoff-standing-and-successor-selection.md` reviews accepted NW-135 / PR #51 against the PC4 PM handoff and accepted NW-134 boundary. It finds `/web-admin/operational/handoff` sufficient for synthetic proof, marks PC4 `synthetic-demo-ready, not proof-complete, not real-production-ready`, records subject UUID display and trace/copy as candidate-only proof-friction pressure, and selects exactly one successor: NW-137 to run the PC4 synthetic walkthrough/proof using S25 worker transfer as primary, S27 logistics handoff as cross-check, and S22 campaign reassignment only as secondary continuity evidence. Validation: docs-only `git diff --check`, `rg "NW-136" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, artifact file check, NW-137 successor prompt file check, and `rg "NW-137" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md docs/agent-working-surface/prompts/NW-137-run-pc4-synthetic-walkthrough-proof.md` passed. Runtime tests skipped because NW-136 changes only a non-authoritative product-validation artifact, prompt routing, backlog/status trace, and artifact indexing; no runtime code, tests, contracts, schemas, migrations, CI behavior, validation policy, product spec, platform spec, BAR, CDL, gap register, mobile code, server/web-admin implementation, lab state, real-production approval, or real users/data changed. Gap register touched: no. Artifact trace touched: yes. Active control panel updated: yes. Runtime implementation changed: no.
- NW-137 is accepted: `docs/agent-working-surface/artifacts/NW-137-pc4-synthetic-walkthrough-proof.md` records one synthetic/non-sensitive PC4 owner-review proof over `/web-admin/operational/handoff`. The proof uses S25 worker transfer as the primary example, S27 logistics handoff as the domain-neutral cross-check, and S22 campaign reassignment only as secondary continuity evidence. Beat classifications are `PASS` for accepted NW-135 implementation/test evidence, `FRICTION` for raw subject UUID display and trace-target copy clarity, `NOT_RUN` for live browser/manual runtime inspection, and `OUT-OF-SCOPE` for broad reporting/import/export/warehouse/API/catalog/filter/cadence/percentages/completion/all-clear, queue/list/batch/automation/reassignment/fallback authority, retention/security/offboarding, pattern, new-scope, entity lifecycle, tenant/control-plane, PC2 lab, real users/data, runtime, contracts/schemas/sync, CI, BAR, CDL, gap-register, or validation-policy work. PC4 remains `synthetic-demo-ready, not real-production-ready`; no real users/data, real-production approval, live proof, lab proof, runtime implementation, tests, contracts, schemas, CI, validation policy, product/platform specs, BAR, CDL, or gap-register changes were selected. NW-137 recommends exactly one next move: NW-138, one small bounded PC4 handoff subject UUID and trace-target copy polish. Evidence: docs-only validation with `git diff --check`, `rg "NW-137" docs/status.md docs/agent-working-surface/platform-next-work-backlog.md`, artifact file check, grep for `synthetic-demo-ready`, and NW-138 prompt file check passed. Runtime tests skipped because no runtime code, tests, contracts, CI behavior, validation policy, or product scope changed. Artifact trace touched: yes. Gap register touched: no. Active control panel updated: yes. Runtime implementation changed: no.
- NW-138 is accepted: polished the accepted `/web-admin/operational/handoff` PC4 handoff page so raw subject identifiers are labeled as `Scoped subject UUID` with helper copy and the trace target is labeled as existing scoped operational context rather than broad history. The change preserves the existing web-admin session, `web_admin.access`, and `web_admin.read_scoped` gates; assignment-scope-before-selection; no-leakage behavior; read-only/no-mutation behavior; resolver-unassigned/no fallback authority; and product-safe wording. It changes only the handoff Thymeleaf template and focused integration assertions; no service, controller, access, authority, query, data-selection, contract, schema, sync, product/platform spec, validation policy, CI, BAR, CDL, gap-register, mobile, lab, real users/data, or production approval changed. Validation: `git diff --check` passed from `/home/hamza/datarun-platform`; `docker compose -f docker-compose.test.yml up -d test-db` passed from `/home/hamza/datarun-platform` with only existing compose warnings; `./mvnw -Dtest=WebAdminOperationalHandoffIntegrationTest test` passed from `/home/hamza/datarun-platform/server` with 6 tests, 0 failures, 0 errors, 0 skipped in 15.18 s, Maven total 20.242 s. Full `./mvnw test` skipped because NW-138 changed only template copy and focused test assertions, not service/controller/access/data-selection behavior. PC4 remains `synthetic-demo-ready, not real-production-ready`; the PC4 proof/polish route is parked and no successor prompt was added. Gap register touched: no. Artifact trace touched: no. Active control panel updated: yes. Runtime implementation changed: yes.
- The post-audit control-plane reset checkpoint is accepted at `docs/checkpoints/checkpoint-2026-06-20-post-audit-control-plane-reset.md`: NW-104 through NW-109 are complete, product implementation is no longer frozen by reset debt, and the next action is to select one bounded product NW through the PC1 PM handoff and PM Product Planner/lane-selector flow. No product scope, runtime behavior, architecture authority, contracts, validation policy, CI behavior, analyzer hard-gate standing, PC1 smoke gate standing, BAR, CDL, gap register, or artifact trace changed.
- NW-037 accepted the bounded production-auth foundation: explicit `(issuer, subject) -> actor_id` principal binding, authenticated actor context, `/api/auth/me`, production-mode push actor binding, and mobile actor alignment. Use `docs/specifications/platform/production-auth-principal-binding.md` for the durable behavior boundary and the NW-037 backlog row for evidence; IDR-027 remains historical provenance.
- NW-038 accepted the server-side OIDC/JWKS auth-provider boundary: configured asymmetric JWT validation by issuer, audience, and JWKS URI behind `AuthenticatedActorResolver`, with explicit `(issuer, subject) -> actor_id` binding as the only actor mapping. Use the NW-038 backlog row for evidence.
- FP-010 is resolved; platform payload schemas are runtime contracts, not deployer shape rows.
- NW-039 selected deployment-managed principal-binding provisioning as the first production administration path. The durable current behavior is in `docs/specifications/platform/production-auth-principal-binding.md`; IDR-028 remains historical provenance for bootstrap, create, rotate, deactivate, rebind/correction, audit, idempotency/concurrency, and append-only operation-history context.
- NW-040 accepted deployment-managed production principal-binding provisioning: append-only operation audit/history, active binding projection/support rows, manifest validation, idempotency, PostgreSQL advisory-lock serialization, create/rotate/deactivate/rebind behavior, optional startup manifest runner, and continued group/claim/JWT `actor_id` non-authority. Use `docs/specifications/platform/production-auth-principal-binding.md` for durable behavior and the NW-040 backlog row for evidence.
- FP-011 is resolved and BAR-104 is baseline-accepted for production OIDC/JWT/Keycloak authority. Future IdP group/claim authority or provisioning-input semantics remain separate future decisions; NW-037 through NW-040 did not make groups, roles, JWT `actor_id`, or IdP claims direct platform authority.
- NW-041 accepted a 2026-06-04 informal-gap baseline assessment checkpoint. The accepted activity role-action boundary is now in `docs/specifications/platform/assignment-scope-and-administration.md`: activity role-actions are only `capture`, `review`, `alert`, `task_created`, and `task_completed`; `assignment_changed` remains assignment administration, not an activity-role action.
- NW-057 accepted the context expression property boundary, now recorded in `docs/specifications/platform/expression-language.md`: unknown `context.*` refs are deploy-time invalid, and the only accepted form-context refs are the seven fixed platform properties. Runtime evaluators remain null-safe for missing values, but config publication must use the fixed whitelist.
- No baseline acceptance candidate is currently active. NW-042 accepted the constrained S22 coordinated distribution campaign runtime probe without adding new platform primitives; use the NW-042 backlog row for exact evidence. NW-047 accepted `docs/agent-working-surface/operational-ux-layering-companion.md` as a non-authoritative operational UX/product vocabulary and layering guardrail; future operational UI, reporting, workflow, and product-design slices should use it before design/code while preserving CDL/BAR/contracts as authority. NW-043 accepted `docs/agent-working-surface/artifacts/NW-043-assignment-admin-authority-exploration.md`: current containment-only assignment administration remains sufficient for accepted baseline/S22/S25-style responsibility management, but too coarse for broad production assignment-admin exposure. NW-048 selected platform-owned `assignment_admin.create` and `assignment_admin.end` command capabilities outside `activities[*].roles`, evaluated server-side from a deployment-configured `assignment_admin_capabilities` role-to-command policy before same-assignment containment; current durable behavior is in `docs/specifications/platform/assignment-scope-and-administration.md`, with IDR-029 as historical provenance. NW-050 implemented that server-side command-capability policy and same-assignment command-plus-containment enforcement; one active assignment must both grant the command and contain the requested create/end scope. NW-049 accepted `docs/agent-working-surface/artifacts/NW-049-access-exceptions-shared-device-scope-exploration.md` as a focused routing artifact: keep NW-050 settled, model only simple current-scope auditor visibility as ordinary assignments, keep grace behavior accept-and-flag plus advisory, keep IdP group/claim authority rejected, and split successors into NW-051 special read/write access, NW-052 shared-device session lifecycle, NW-053 subject/query/custom scope under BAR-108, and NW-054 device data expiry/retention under BAR-106. NW-051 accepted `docs/agent-working-surface/artifacts/NW-051-special-read-write-access-boundary-routing.md`: simple current-scope auditor visibility remains ordinary assignment/config posture; broad audit/history read surfaces, redacted/no-local-retention audit views, cross-axis/dynamic auditor scope, and emergency/special write bypasses are explicitly deferred until a concrete product/security decision promotes them; emergency or temporary domain work must use time-bounded assignments plus activity role-action permissions; NW-050 remains assignment create/end only. Current shared-device session/local-state behavior is now durable in `docs/specifications/platform/shared-device-session-and-local-state.md`; IDR-030/NW-052 remain historical decision provenance, and NW-055 remains implementation evidence for mobile actor partitions and server actor-scoped device sync bookkeeping. NW-054 remains the BAR-106 route for expiry, device decommissioning, sealed-partition recovery, token/session retention policy, local encryption, and broader retention/security. NW-044 through NW-046 remain future-decision exploration routes for reporting aggregation/import-export, domain conflict automation/batch resolution, and generic flag cascade/pattern traversal reporting. Entity lifecycle, trigger/reporting expansion, broad audit/history APIs, emergency override semantics, auto-resolution, resolver reassignment, online production binding-admin APIs, IdP group/claim authority, mobile authoritative rejection, and new scope mechanisms remain future-decision follow-ups.
- First-deployment work routes through `docs/agent-working-surface/first-deployment-task-packet-router.md`. The workshop is closed and consolidated into `docs/workshops/first-deployment/summary.md`; removed stage, role, task-planning, and gate-review files remain provenance in git history only.
- NW-059 is accepted in commit `8692607`: mobile sync presentation now distinguishes saved-local, waiting, syncing, synced, and failed states over existing `AppState`/`SyncResult` state, and failed attempts no longer update the last successful-sync timestamp. Focused tests passed 13 cases, the full mobile suite passed 114 tests, and touched-file analysis is clean.
- NW-060 is accepted in commit `5dad1c9`: successful offline capture returns to the surviving screen, refreshes existing projection/pending state, confirms saved-local/waiting status, and keeps pending work visible from the work list with access to the existing sync panel. Focused tests passed 12 cases, the full mobile suite passed 119 tests, and touched-file analysis is clean. It changed no contracts, persisted state, sync semantics, authority, retention, or S06 entity lifecycle.
- NW-061 is accepted in commit `1b4ca81`: the work list distinguishes get-work, syncing, retry, missing setup/forms, missing assignment, and ready-to-capture states from existing local config, assignment, and sync state. Existing records and pending-local status remain visible, and configured capture remains advisory rather than locally blocked when assignment state is absent. Focused tests passed 19 cases, the full mobile suite passed 126 tests, and touched-file analysis is clean. It changed no persisted readiness state, contracts, sync semantics, login, authority, retention, or S06 entity lifecycle.
- NW-062 is accepted in commit `9e8670a`: eligible captures expose a prefilled correction form that appends a distinct same-subject/exact-shape/activity capture while preserving the original event in history. Reverted edits do not append duplicates, and alias-aware history preserves the selected event's subject ref. Focused tests passed 10 cases, the full mobile suite passed 131 tests, and touched-file analysis is clean. It introduced no event mutation, durable correction linkage, envelope/type/payload metadata, shape-version inference, sync/authority semantics, or S06 entity lifecycle.
- NW-063 is accepted: `docs/agent-working-surface/artifacts/NW-063-production-deployment-ops-hardening-map.md` selects a single Linux application host behind external TLS with durable external PostgreSQL 16 as the first portable reference target. It does not claim the current image or compose file is production ready.
- NW-063 found a blocking packaging drift: a clean build mirroring the current `server/` Docker context skips root `contracts/` resources, so the JAR omits required workflow pattern definitions and platform payload schemas. NW-056's broad Docker-runnable wording must not be used as clean-image evidence until NW-065 fixes and tests packaging.
- NW-064 is accepted by Hamza and amended for the actual solo-owner model: `docs/operations/policies/first-reference-deployment-policy.md` sets provider-neutral ownership, 1-hour RPO, 8-hour RTO, backup/restore, mounted-secret, SLO/alerts, support/incident, release/recovery, evidence, and `Asia/Aden` controls. Hamza may hold all rehearsal and initial owner-operated production roles with evidence-backed self-approval. Independent human continuity is explicitly unproven and becomes required only on a concrete staffing, contractual, regulatory, organizational, or service-coverage trigger. Real production remains separately blocked on provider/region, jurisdiction/data classification, communication/login, and organizational review.
- NW-065 is accepted in commits `617f895`, `e458b73`, `b6d6515`, and `3dc09b5`: the release image now packages canonical contract resources and runs non-root with OCI identity; the production profile is fail-closed with bounded health, Prometheus, structured logs, hidden development surfaces, and graceful shutdown; `deploy/reference/` provides digest-pinned single-host assets with loopback-only listeners, read-only mounted configuration, PostgreSQL TLS verification, resource controls, and preflight checks; and non-web one-shot tooling applies accepted principal bindings, complete reviewed config snapshots, and the exact initial assignment bootstrap without new APIs or authority. The full server suite passed 339 tests, focused provisioning/auth/config/assignment suites passed 125 tests, and the final image/resource/startup and full digest-selected deployment preflights passed against PostgreSQL 16. This is synthetic reference tooling, not proof of backup, restore, monitoring, rotation, incident response, or real-production approval.
- NW-066 is accepted in commits `c4698f6` and `2cf6bd3`; its indexed production deployment runbook and reusable rehearsal plan use the accepted NW-064 policy and exact NW-065 surfaces, distinguish forward migration, proven-compatible application rollback, database restore, and forward fix, and measure RPO/RTO against a frozen synthetic source and verified recovery point. The 2026-06-14 NW-067 owner amendment aligns the gate with solo operation: provider-specific DNS/TLS, PostgreSQL recovery, secrets, OIDC token, monitoring/alert, evidence, previous/candidate images, compatibility evidence, and a fresh-session solo cold-recovery setup remain mandatory; a second human operator is not required. This accepts the procedure, not backup/restore/monitoring/rotation evidence, independent human continuity, or real-production approval.
- NW-067 is accepted in `docs/operations/rehearsals/2026-06-17-production-deployment-reference-environment.md` by composite synthetic reference-environment evidence. R1-R9 produced clean install, migration repeatability after adapter correction, TLS/OIDC negative checks, provisioning idempotency, sync smoke, measured restore timing (`rpo_seconds=4`, `rto_seconds=214`), previous-to-candidate upgrade, and failed-start containment. Accepted successor adapters NW-075 through NW-081 cover backup encryption, DB credential rotation, Keycloak/JWKS signing-key rotation, monitoring/alert delivery, fresh encrypted recovery point/RPO, and fresh-session protected-smoke token path. The 2026-06-18 R12 rerun at `docs/operations/rehearsals/2026-06-18-production-deployment-r12-fresh-session-rerun.md` passed with app readiness/liveness `UP`, all monitoring targets `up`, no active critical/backup alerts, DB1 Flyway V1-V10 with 8 events and max watermark 9, encrypted backup `20260617-022519F_20260618-131808D` at RPO age `1981` seconds, protected `/api/auth/me`, `/api/sync/config`, and `/api/sync/pull` HTTP 200, token cleanup, and clean evidence secret scans. This accepts the synthetic reference deployment rehearsal only; real production and independent human continuity remain unproven.
- NW-075 is accepted in `docs/operations/rehearsals/2026-06-17-encrypted-backup-pitr-adapter.md`: pgBackRest reports `cipher: aes-256-cbc` for fresh bucket `datarun-nw075-backup`, DB2 restored from that encrypted repository, restored DB counts match source counts, and restored app readiness plus `/api/auth/me`, `/api/sync/config`, and authorized pull smoke passed using the active `field-worker-rotated` synthetic principal. The original `field-worker` token is expected 401 evidence because its subject is no longer active in principal binding.
- NW-076 is accepted in `docs/operations/rehearsals/2026-06-17-db-credential-rotation-adapter.md`: the app rotated through synthetic DB runtime roles g1 and g2 using mounted config-tree directories, g1 and g2 both passed readiness/auth/config/pull smoke, g1 was disabled and rejected from the app host, and g2 is the active runtime credential. The original `datarun_runtime` role remains available as recovery.
- NW-077 is accepted in `docs/operations/rehearsals/2026-06-17-keycloak-jwks-rotation-adapter.md`: Keycloak added a new higher-priority generated RSA signing provider, new-key tokens were accepted by the reference app, the old provider was made passive while remaining enabled for overlap, and both old-token and new-token auth smoke returned HTTP 200. No IdP groups, roles, resource claims, or JWT `actor_id` became platform authority.
- NW-078 is accepted in `docs/operations/rehearsals/2026-06-17-monitoring-alert-adapter.md`: Prometheus, blackbox-exporter, and node-exporter on the app host plus Alertmanager and a Hamza-approved synthetic webhook recipient on the ops host represented readiness, database, JWKS, backup freshness/encryption, certificate, and capacity signals. The final annotated inject delivered all six synthetic alert groups with owner/runbook labels and response/escalation annotations, then delivered resolved notifications after cleanup. Final Prometheus and Alertmanager checks reported zero active alerts.
- NW-080 is accepted in `docs/operations/rehearsals/2026-06-18-encrypted-backup-recovery-point-refresh.md`: DB1 created encrypted pgBackRest diff backup `20260617-022519F_20260618-131808D` in the accepted `datarun-nw075-backup` repository with `cipher: aes-256-cbc`, latest recoverable time `2026-06-18T13:18:14Z`, RPO age `66` seconds, backup metric provenance from the real stop timestamp, resolved `DatarunBackupStale` webhook evidence, and zero active backup/critical alerts.
- NW-081 is accepted in `docs/operations/rehearsals/2026-06-18-fresh-session-protected-smoke-token-path.md`: the fresh-session path strips CR/LF from the root-owned synthetic rotated-worker password file into a temporary `/dev/shm` file, obtains a short-lived Keycloak direct-grant token for `datarun-cli`, retains only redacted JWT metadata, and proves `/api/auth/me`, `/api/sync/config`, and `/api/sync/pull` with HTTP 200 while cleaning token material.
- S06/entity lifecycle remains visible as BAR-105/NW-021 future-decision work. It did not block NW-060, NW-061, or NW-062 because those slices add no known-set, candidate, lifecycle, duplicate, merge, split, or subject-link authority.
- Default implementer context is `AGENTS.md`, this section, the selected task/NW packet, the exact touched files, and nested `server/AGENTS.md`, `mobile/AGENTS.md`, or `contracts/AGENTS.md` only when those surfaces are in scope.
- Historical phase detail, active decision text, architecture docs, scenarios, and exploration archives are not default context. Open them only when the task surface, a touched file, or a drift investigation routes you there.

---

## Current Phase And Historical Detail

This section preserves historical phase and implementation detail. It is not
the startup source order for agents; use **Current Routing** above and the
active working-surface routers for current authority and work selection.

**Phase 3: Configuration** — **COMPLETE** (including 3d close-out and 3e envelope-type retrofit)

| Sub-phase | Status | Notes |
|-----------|--------|-------|
| **3a: Shapes + Config Delivery** | **Complete** | 80 server + 33 mobile tests. |
| **3b: Expressions + DtV** | **Complete** | 148 server + 47 mobile tests. |
| **3c: Config Packager + Full Pipeline** | **Complete** | 153 server + 54 mobile tests. |
| **3d: Close-out** | **Complete** | 153 server + 67 mobile tests. activity_ref plumbing, sensitivity surface on device, ContextResolver. |
| **3e: Envelope Type Vocabulary Retrofit** | **Complete** | 164 server + 72 mobile tests. Executes [ADR-007](adrs/adr-007-envelope-type-closure.md): envelope `type` closed at 6 values; four identity/integrity facts are platform-bundled shapes. |

**Phase 4: Workflow & Policies** — **COMPLETE** (completion audit green)

Phase 4.0 (role-action enforcement) was drafted and rolled back. IDR-020 through IDR-030 remain accepted implementation provenance and verification inputs, not new decision-routing homes. They cover pattern state representation, role-action enforcement, flag severity and domain uniqueness, role-action domain boundaries, multi-axis assignment containment, pattern definition delivery, conflict resolver routing, production principal binding, principal-binding administration, assignment-admin command capability, and shared-device session lifecycle. The Phase 4 implementation spec is at [phase-4.md](implementation/phases/phase-4.md). Phase 4.1 role-action work has landed across server-side config validation/package preservation, authoritative `role_stale` action-authority semantics, assignment-administration boundary checks, and mobile advisory role-action gating. Phase 4.2 flag severity has landed across platform defaults, deployment-wide L0 overrides, package delivery, and server/mobile effective severity interpretation. IDR-024 / FP-007 assignment containment hardening has landed across multi-axis create/end authority, explicit initial bootstrap semantics, empty subject/activity rejection, and null-activity work-event authorization. FP-008 assignment command actor identity binding has landed so `/api/assignments` create/end evaluates authority for the authenticated token actor, not request-body actor IDs; the unauthenticated HTML admin assignment console is documented as development-only and no longer takes command actor IDs from assignment forms. Phase 4.3 domain uniqueness has landed across deploy-time shape validation, config package preservation, server accept-and-flag detection, duplicate-basis exclusion for unresolved flags, accepted-resolution re-inclusion, and mobile advisory duplicate checks. Phase 4.4 pattern registry/binding validation has landed across the platform-bundled binding metadata registry, deploy-time activity binding validation, and mobile raw binding preservation. IDR-025 pattern definition delivery has landed across canonical contract files, server resource loading, config package delivery, and mobile packaged-definition preservation. Phase 4.5 enabled-binding pattern-state projection has landed for `capture_with_review/v1`, `multi_step_approval/v1`, `transfer_with_acknowledgment/v1`, and `ongoing_resolution/v1` using rebuildable/on-demand server and mobile projection, shared fixtures, universal state fields, and unresolved/accepted/rejected flag exclusion semantics. FP-005 subject-history backfill has landed as a separate `/api/sync/subject-history` surface with independent cursor pagination, request-time authorization on every page, merge-alias and split-lineage coverage, activity filtering, and no mutation of normal live-sync watermarks. `ongoing_resolution/v1` projection now consumes that backfill surface for newly assigned long-running subjects. FP-009 runtime resolver enforcement has landed across resolver designation, bearer-bound conflict APIs, canonical resolution checks, and unauthorized-resolution flagging. Phase 4.6 transition detection has landed with IDR-020 transition matching, IDR-026 resolver routing, legal-transition-only accepted re-inclusion, and no auto-resolution. The P04 scenario-grade Responsibility Binding gate has landed with coordinated campaign, overlapping responsibility area, mid-campaign reassignment, scope-filtered sync, and role-action boundary coverage. Post-Phase-4 scenario probes now add accepted runtime evidence for S19 offline stale authority, S00 structured capture, S21 scoped supervisor review, S23 setup/config, S26 reporting/aggregate oversight, and S27 non-health logistics transfer. The completion audit found all applicable Phase 4 gates green; S06/entity lifecycle remains explicitly deferred.

### Carried architectural debt — ADR-007 + Phase 3e retrofit

A Phase 3d close-out audit (2026-04-21) found that Phases 1-2 persisted four string literals (`conflict_detected`, `conflict_resolved`, `subjects_merged`, `subject_split`) as envelope `type` values, contradicting the closed envelope `type` vocabulary now governed by the CDL. [ADR-007](adrs/adr-007-envelope-type-closure.md) remains retrofit provenance: those four strings are internal **shape** names, not envelope types. Phase 3e landed the code retrofit. New code must not key on those strings as envelope types.

### Flagged Positions Register (Legacy Provenance)

[`docs/flagged-positions.md`](flagged-positions.md) is a legacy gated-risk and
provenance register. It is not the current source for new routing pressure.
Route new pressure through the gap playbook, BAR/NW, and the correct durable
home. Consult FP rows only when a current route names one, the touched surface
matches an FP topic, or a drift/provenance audit needs the historical gate.
State as of 2026-06-17:

| FP# | Item | Blocks | Severity | Status |
|-----|------|--------|:--------:|--------|
| FP-001 | `role_stale` projection-derived role verification | IDR-021 | A | **RESOLVED** |
| FP-002 | `subject_lifecycle` table read-discipline audit | Phase 4 | B | **RESOLVED** |
| FP-003 | Envelope schema parity test | — | C | **RESOLVED** (EnvelopeSchemaParityTest) |
| FP-004 | `assignment_ref` as potential future envelope field | BAR-107 / architecture gap route for future assignment-targeting pressure | B | **SUPERSEDED** as active tracker; underlying pressure remains `future_decision` |
| FP-005 | Scoped pull temporal anchor and subject-history backfill | Phase 4 `ongoing_resolution` | A | **RESOLVED** |
| FP-006 | `temporal_authority_expired` superseded-assignment false positive | Phase 4 role-action / detection ordering | A | **RESOLVED** |
| FP-007 | Multi-axis assignment containment and null-activity semantics | Phase 4 assignment-administration hardening | A | **RESOLVED** |
| FP-008 | Assignment command actor identity binding | Phase 4.3 entry / assignment command exposure | A | **RESOLVED** |
| FP-009 | Conflict resolver designation and single-writer resolution enforcement | Phase 4.6 `transition_violation`; runtime resolver enforcement / auto-resolution | A | **RESOLVED** |
| FP-010 | Platform-bundled payload shape contract parity | platform payload shape changes / production contract-hygiene close-out | C | **RESOLVED** |
| FP-011 | Authentication principal-to-actor mapping and group non-authority | production Keycloak/OIDC/JWT integration; group/claim authority models | B | **RESOLVED** (future group/claim authority requires successor decision) |

FP trigger/gate mechanics are preserved in active surfaces as explicit triggers,
exit conditions, and evidence links rather than new FP rows.

### Previous Phases

| Phase | Status | Tests |
|-------|--------|-------|
| **0: Core Loop** | Complete | Foundation |
| **1: Identity & Integrity** | Complete | 64 total |
| **2: Authorization & Multi-Actor** | Complete | 80 server + 22 mobile |
| **3a: Shapes + Config Delivery** | Complete | 80 server + 33 mobile |
| **3b: Expressions + DtV** | Complete | 148 server + 47 mobile |
| **3c: Config Packager + Full Pipeline** | Complete | 153 server + 54 mobile |
| **3d: Close-out** | Complete | 153 server + 67 mobile |
| **3e: Envelope Type Vocabulary Retrofit** | Complete | 164 server + 72 mobile |

---

## What's Built

- `contracts/` — envelope schema (11 fields, closed 6-type envelope vocabulary per ADR-4 S3 and ADR-007, Draft 2020-12), sync protocol (extended: push `device_id`/`last_pull_watermark`/`flags_raised` plus production-auth actor binding, pull bearer-token scope plus `config_version`/`has_more`, and separate subject-history backfill), deployer `shape-format` schema, server/mobile config-package schema, platform payload shape schemas: assignment_created/v1, assignment_ended/v1, conflict_detected/v1, conflict_resolved/v1, subjects_merged/v1, subject_split/v1 (bundled into server runtime for payload validation; not deployer shape registry rows), pattern definition schema plus canonical platform pattern definitions under `contracts/patterns/`
- `server/` — Spring Boot app: event store, sync push/pull, subject projection, envelope validation
- `server/authorization/` — AssignmentService (create/end with accepted assignment-admin command-capability checks plus multi-axis scope containment, explicit initial bootstrap path, and target-assignment end authority), AssignmentAdminCapabilityService (server-side `assignment_admin_capabilities` policy lookup/validation), ScopeResolver (authority reconstruction from event timeline, 3 scope types), AuthenticatedActorResolver (dev-token or accepted explicit `(issuer, subject) -> actor_id` binding), ActorTokenInterceptor (Bearer credential auth on `/api/auth/me`, sync push/pull/subject-history/config, assignment, and conflict APIs), ActorTokenRepository (SecureRandom 32-byte hex tokens, revocation), AuthPrincipalBindingRepository, JwtPrincipalTokenValidator (local HS256 test/foundation validator behind the resolver boundary), OidcJwksTokenValidator (configured issuer/audience/JWKS asymmetric JWT validation behind the resolver boundary plus nonce validation for web-admin login tokens), WebAdminSecurityFoundationConfig, WebAdminSessionController/Service (separate `/web-admin` login/session shell), LocationRepository (materialized path hierarchy), SubjectLocationRepository, ActiveAssignment (isActive, containsGeographically/Subject/Activity with null activity not authorized by activity-restricted assignments), WebConfig, REST controllers for auth-me/assignments/locations/dev-token admin
- `server/identity/` — ServerIdentity (env var + DB fallback, SEQUENCE-backed device_seq), IdentityLifecycleProjection (event-derived lifecycle), SubjectAliasProjection (rebuildable `subject_aliases` projection), IdentityService (merge/split with subject-scoped advisory locking), IdentityController (REST endpoints)
- `server/integrity/` — ConflictDetector (per-event W_effective detection + stale_reference detection + auth CD: scope_violation, temporal_authority_expired, role_stale), DomainUniquenessDetector (`domain_uniqueness_violation` accept-and-flag for `shapes[*].uniqueness`), TransitionViolationDetector (`transition_violation` accept-and-flag for active pattern bindings), ResolverRoutingService (IDR-026 resolver designation), FlagCatalog (fixed resolvability + severity defaults), ConflictSweepJob (5-min stateless sweep), ConflictResolutionService (canonical single-writer resolve: accepted/rejected/reclassified + unauthorized-resolution flagging + manual identity_conflict flags + effective severity surfaces), ConflictController (bearer-bound REST: resolve, actor-filtered flag list, create identity_conflict)
- `server/sync/` — Two-Tx pipeline (TransactionTemplate: Tx1 persist, Tx2 identity CD + auth CD + domain uniqueness + transition detection + flags). Production-auth push requires bearer-authenticated actor binding and rejects client-authored `system:*`, missing, invalid, or mismatched `actor_ref.id` before persistence. Pull: scope-filtered with post-query activity + subject_list filtering (AND within assignment, OR across assignments), with `device_sync_state` pull/config bookkeeping keyed by `(device_id, actor_id)`. Subject-history backfill: separate bearer-authenticated `/api/sync/subject-history` cursor surface for subject/activity projection repair; it checks current assignment authorization on every page and does not mutate `device_sync_state`.
- `contracts/flag-catalog.md` — 9 flag-category slots: implemented identity/authorization categories, Phase 4 `domain_uniqueness_violation` and `transition_violation`, plus one reserved slot
- `server/subject/` — SubjectProjection with flag exclusion + alias resolution + assignment_changed exclusion (CTE-based, LEFT JOIN subject_aliases)
- `server/subject/` — SubjectController with alias-aware event retrieval (includes events from all alias chains)
- `server/event/` — EventRepository extended: location_path denormalization on insert, findSinceScoped (3-category OR: geo subjects, own assignments, system events), subject-history backfill page query, findByType
- `server/projection/` — rebuildable PatternStateProjection for enabled pattern bindings, deriving state from active activity bindings, contract-backed PatternRegistry definitions, and authoritative flag-resolution state without durable workflow tables
- `mobile/data/` — SyncService bearer push/pull/config, `/api/auth/me` actor refresh, DeviceIdentity server-resolved actor-session storage, per-actor local SQLite partitions for mutable events/projections/assignments/config state, actor-scoped normal pull watermarks and subject-history cursors, drain-or-seal shared-device switching, and PatternProjectionEngine counterpart reading ConfigStore activity bindings plus packaged `pattern_definitions`, with shared fixture equivalence for enabled patterns
- Migration V1: events table (BIGSERIAL sync_watermark)
- Migration V2: server_identity table, server_device_seq SEQUENCE, device_sync_state table
- Migration V3: subject_aliases table (with CHECK constraint)
- Migration V4: locations (materialized path), actor_tokens, subject_locations, events.location_path column, covering index (idx_events_scoped_pull), assignment expression index
- Migration V6: deployment_config table for deployment-wide L0 `flag_severity_overrides`
- Migration V8: `auth_principal_bindings` table for production-auth principal-to-actor lookup
- Migration V10: `device_sync_state.actor_id` and `(device_id, actor_id)` primary key for actor-scoped shared-device pull/config bookkeeping
- Admin UI: subject list, flag list, development-only assignment list/creation form, production `/web-admin/assignments` create/end workflow, production `/web-admin/operational` scoped view/report/handoff read surfaces, location hierarchy browser
- `docker-compose.yml` — full stack dev setup
- `docker-compose.test.yml` — test DB with host networking
- `.github/workflows/server-ci.yml` — GitHub Actions CI

---

## What's Next

**Post-Phase-4 stabilization** — active. Phase 4 is complete; the active status surfaces are the working-surface BAR/backlog, not old review chronology. BAR-001 through BAR-015 and BAR-104 are accepted, including BAR-010 config package delivery and BAR-104 production OIDC/JWT/Keycloak authority. The scenario runtime probe set selected and routed from NW-023 has landed: NW-025/S19, NW-026/S00, NW-029/S21, NW-030/S27, NW-032/S23, and NW-033/S26. NW-034 added contract hygiene for deployer shape format and config-package schemas. NW-037 added the production-auth principal binding foundation. NW-038 added server-side OIDC/JWKS provider validation. NW-039 selected deployment-managed binding provisioning for production principal-binding administration. NW-040 implemented that provisioning/audit path and resolved FP-011/BAR-104 for the current production-auth gate. For accepted production-auth behavior, use `docs/specifications/platform/production-auth-principal-binding.md`; IDR-027 and IDR-028 are historical provenance. For accepted shared-device session/local-state behavior, use `docs/specifications/platform/shared-device-session-and-local-state.md`; IDR-030 is historical provenance and NW-055 is implementation evidence.

- IDR-021 (Role-Action Enforcement Model) is accepted implementation provenance. It builds on FP-001's role timeline check and keeps FP-005 out of role-action scope: live sync stays request-time scoped, subject-history backfill is separate Phase 4 `ongoing_resolution` work, and audit/historical pull is out of Phase 4 live sync unless a successor decision introduces a separate pull class/API.
- IDR-022 (Flag Severity + Domain Uniqueness) is accepted implementation provenance. It defines deployment-wide L0 severity overrides through `flag_severity_overrides`, keeps resolvability platform-owned, defines shape-declared `domain_uniqueness_violation`, and keeps FP-005 backfill/audit behavior out of scope.
- IDR-023 (Role-Action Domain Boundary and Assignment Administration) is accepted implementation provenance. It excludes `assignment_changed` from `activities[*].roles`; assignment create/end remains an online authority path, not an activity role-action work event.
- IDR-024 (Multi-axis Assignment Containment) is accepted implementation provenance. It closes the decision gap around assignment creation/end containment across `geographic`, `subject_list`, and `activity`, defines explicit bootstrap/root semantics, and states that activity-restricted assignments do not authorize ordinary null-activity work events.
- IDR-025 (Pattern Definition Contract and Delivery) is accepted implementation provenance. It makes `contracts/patterns/*.json` the canonical platform-owned pattern source, loads those definitions into the server registry, and delivers referenced definitions through the atomic config package for mobile runtime use.
- IDR-026 (Conflict Resolver Routing and Single-Writer Resolution) is accepted implementation provenance. It defines resolver routing for all active/imminent flag categories, canonical resolution semantics, production conflict API actor binding, and defers resolver reassignment and auto-resolution mechanics.
- IDR-027 (Production Auth Principal-To-Actor Binding) is accepted implementation provenance. It defines explicit principal binding, forbids group/claim/JWT `actor_id` direct authority, and now includes the NW-038 OIDC/JWKS validation mode.
- IDR-028 (Production Principal-Binding Administration) is accepted implementation provenance. It selects deployment-managed manifest provisioning for production binding administration, defers online admin APIs, and NW-040 implemented the selected provisioning/audit path.
- IDR-029 (Assignment-Admin Command Capability) is accepted implementation provenance. It selects platform-owned `assignment_admin.create` and `assignment_admin.end` command capabilities outside `activities[*].roles`, with NW-050 implementing server-side command-plus-containment enforcement.
- IDR-030 (Shared-Device Session Lifecycle) is accepted implementation provenance. The current durable behavior target is `docs/specifications/platform/shared-device-session-and-local-state.md`, covering single-active-actor shared-device sessions, drain-or-seal actor switching, per-actor local partitions, actor-scoped sync progress, and BAR-106 deferral for broader retention/security.
- Phase spec: [docs/implementation/phases/phase-4.md](implementation/phases/phase-4.md) (reconciled; tracks landed Phase 4.1 role-action gates, Phase 4.2 severity gates, Phase 4.3 uniqueness gates, Phase 4.4 registry/binding gates, Phase 4.6 transition gates, and P04 scenario-grade Responsibility Binding coverage)
- Landed implementation slices: Phase 4.1 role-action enforcement from IDR-021/IDR-023, including authoritative server `role_stale` action-authority semantics and mobile advisory role-action gating; Phase 4.2 flag severity defaults and deployment-wide L0 `flag_severity_overrides` from IDR-022; IDR-024 / FP-007 assignment administration hardening; FP-008 assignment command identity binding; Phase 4.3 domain uniqueness schema, detector, and mobile advisory uniqueness from IDR-022; Phase 4.4 platform pattern registry and activity binding validation from IDR-020; IDR-025 pattern definition contract/package delivery; Phase 4.5 rebuildable pattern-state projection for enabled bindings including `ongoing_resolution/v1`; FP-005 subject-history backfill; FP-009 runtime resolver designation and single-writer resolution enforcement; Phase 4.6 transition detection; P04 scenario-grade Responsibility Binding coverage; FP-010 platform payload shape contract parity; NW-037/IDR-027 production-auth principal binding foundation; NW-038 OIDC/JWKS auth-provider validation; NW-040 production principal-binding provisioning; NW-050/IDR-029 assignment-admin command-capability enforcement; and NW-055/IDR-030 shared-device actor partitions with durable current behavior now extracted to `docs/specifications/platform/shared-device-session-and-local-state.md`. These do not implement auto-resolution, resolver reassignment, normal sync backfill, durable workflow-state tables, mobile OIDC login UX, online production binding-admin APIs, group/claim authority, sealed-partition recovery, local expiry/decommissioning, or access-exception runtime behavior.
- Recommended next move: execute NW-143 to reconcile the consumed PC2 fixture and select a bounded retry or parking path before any further PC2 live proof attempt. PC1, PC2, PC3, and PC4 remain synthetic-demo-ready only, not real-production-ready. Keep NW-093 blocked unless real-production approval, provider/region/jurisdiction, real users/data, or support commitment becomes active. Keep NW-054 as the retention/security/offboarding route, keep NW-094 through NW-098 separate for control-plane or tenant-aware runtime/storage/sync work, keep NW-044 for broad reporting/import-export, use NW-045 for conflict automation/batch resolution, use NW-053 for new subject/query/custom scope, keep NW-021 for S06/entity lifecycle, and select NW-073 only if future product work actually depends on pattern registry/projection behavior.
- Coverage gap closed: P04 now has scenario-grade coverage for reassignment plus S19 stale offline authority, S00 structured capture, S21 scoped supervisor review, S23 setup/config, S26 reporting/aggregate oversight, and S27 logistics transfer.

### Test Debt (carried from Phase 3)
- Multi-version PE fixture
- activity_ref auto-population
- Widget-level form tests

---

## Blockers

FP-005 is `RESOLVED`; `ongoing_resolution/v1` projection now uses the distinct subject-history backfill surface for newly assigned long-running subjects. FP-009 is `RESOLVED`; Phase 4.6 `transition_violation` emission has landed and uses IDR-026 resolver routing, but auto-resolution and resolver reassignment remain deferred successor work. FP-010 is `RESOLVED`; server runtime now bundles and enforces the six platform payload shape schemas from `contracts/shapes/`, and platform payload schemas are not deployer shape registry rows or packaged as deployer `shapes`. FP-011 is `RESOLVED` for the production OIDC/JWT/Keycloak principal-to-actor and group/claim non-authority gate; future group/claim authority remains a separate decision. BAR-104 is accepted with NW-037/NW-038/NW-040 runtime evidence. The P04/S00/S19/S21/S23/S26/S27 scenario-grade probe gaps selected for stabilization are closed. BAR-010 is accepted; no baseline acceptance candidate is currently active.

---

## Accepted IDR Inputs

| Decision | Status | Reference |
|----------|--------|-----------|
| IDR-020: Pattern State Machine Representation | **ACCEPTED INPUT** | [idr-020-pattern-state-machine-representation.md](decisions/idr-020-pattern-state-machine-representation.md) |
| IDR-021: Role-Action Enforcement Model | **ACCEPTED INPUT** | [idr-021-role-action-enforcement-model.md](decisions/idr-021-role-action-enforcement-model.md) |
| IDR-022: Flag Severity + Domain Uniqueness | **ACCEPTED INPUT** | [idr-022-flag-severity-and-domain-uniqueness.md](decisions/idr-022-flag-severity-and-domain-uniqueness.md) |
| IDR-023: Role-Action Domain Boundary and Assignment Administration | **ACCEPTED INPUT** | [idr-023-role-action-domain-boundary-and-assignment-administration.md](decisions/idr-023-role-action-domain-boundary-and-assignment-administration.md) |
| IDR-024: Multi-axis Assignment Containment | **ACCEPTED INPUT** | [idr-024-multi-axis-assignment-containment.md](decisions/idr-024-multi-axis-assignment-containment.md) |
| IDR-025: Pattern Definition Contract and Delivery | **ACCEPTED INPUT** | [idr-025-pattern-definition-contract-and-delivery.md](decisions/idr-025-pattern-definition-contract-and-delivery.md) |
| IDR-026: Conflict Resolver Routing and Single-Writer Resolution | **ACCEPTED INPUT** | [idr-026-conflict-resolver-routing-and-single-writer-resolution.md](decisions/idr-026-conflict-resolver-routing-and-single-writer-resolution.md) |
| IDR-027: Production Auth Principal-To-Actor Binding | **ACCEPTED INPUT** | [idr-027-production-auth-principal-actor-binding.md](decisions/idr-027-production-auth-principal-actor-binding.md) |
| IDR-028: Production Principal-Binding Administration | **ACCEPTED INPUT** | [idr-028-production-principal-binding-administration.md](decisions/idr-028-production-principal-binding-administration.md) |
| IDR-029: Assignment-Admin Command Capability | **ACCEPTED INPUT** | [idr-029-assignment-admin-command-capability.md](decisions/idr-029-assignment-admin-command-capability.md) |
| IDR-030: Shared-Device Session Lifecycle | **ACCEPTED INPUT** | [idr-030-shared-device-session-lifecycle.md](decisions/idr-030-shared-device-session-lifecycle.md) |
