spring:
  datasource:
    url: jdbc:postgresql://localhost:15432/datarun_test
    username: datarun
    password: datarun
  jackson:
    default-property-inclusion: non_null
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

logging:
  level:
    dev.datarun: INFO
    org.flywaydb: INFO
